# Repo Agent Entry

本文件是本仓库的固定入口。处理任何仓库任务时，必须先读本文件。

## 核心纪律

### 最小改动

- 代码实现必须采用最小改动原则。
- 最小改动不是只改眼前一处、别的都不管；它的含义是边界准确、改动收敛、影响面完整。
- 动手前要把相关源码、调用链、数据流、输出链路和架构边界梳理清楚。
- 对真实受影响的地方，必须改完整，包括输入、核心逻辑、输出、测试和必要文档。
- 对不受影响的地方，一点都不要动。
- 不要基于任务做胡乱引申、联想式发挥或画蛇添足。
- 用户没有明确要求、且不属于真实影响面的内容，不要顺手修改。
- 不要私自新增参数、配置项、运行开关、文件、依赖、脚本或抽象层。
- 不要借任务机会做无关重构、格式化、改名、文档重写或历史清理。
- 如果确实认为必须扩大改动范围，先说明真实依赖关系和原因，并等待用户确认。

### 离线验证

- 改动必须基于项目内部 `stock_trading_advisor/data/backtest_data` 中的股票数据做离线回测验证。
- `backtest_data` 中的正式回测数据使用后复权（`*_hfq.csv`）；普通分析和实际买卖信号默认使用前复权（`qfq`）。
- 正式验证优先使用 `python3 stock_trading_advisor/main.py --report`。
- 针对特定股票或股票集合时，使用 `--report -s/-b ...`，仍然只走内部离线回测数据目录。
- 不要用在线取数结果、实时行情、临时下载数据或外部接口结果替代正式验证。
- 如果本地缺少离线回测数据或依赖导致无法验证，必须明确说明验证未完成，不能把静态检查说成回测通过。

### 基准与同口径红线

- 当前用户确认的原始策略基准由 commit `a8dd63a` 固化，报告是 `stock_trading_advisor/reports/offline_backtest_report_20260906_232005.txt`，股票池是 `backtest_benchmark/codes_250.txt`。未经用户明确确认，不得更换“原始策略”或“原始 baseline”的身份。
- 上述原始报告的 `2.07` 是“全部交易正收益率之和 / 全部交易负收益率绝对值之和”，不是金额口径盈亏比。引用时必须写明公式，禁止改称“总金额盈亏比”。
- 消融版本、手术中间版本、临时候选和参数扫描结果，无论生成时间是否更新、指标是否更接近当前代码，都只能标记为 `intermediate/candidate`，绝不能替代原始 baseline。尤其不得把中间版本的 `1.44` 用作原始策略对照。
- 宣称策略改善前，必须逐项核对并记录：基准 commit、报告路径、股票清单及顺序、数据文件与复权方式、起止日期、成交时点、手续费、未平仓处理、指标名称和精确公式。任一项不同，都不得直接比较数值。
- 如果同时修改回测引擎和策略，必须做三组交叉回测：`A=原策略+原引擎`、`B=原策略+新引擎`、`C=候选策略+新引擎`。只能用 `B -> C` 判断策略效果，用 `A -> B` 判断引擎影响；禁止用 `A -> C` 把两种变化混成“策略提升”。
- 删除或替换原策略代码前，必须保留可执行的 Git 引用，并先生成原策略在新引擎下的同口径报告。无法生成 `B` 时，策略优劣结论必须标记为“不可归因”，不得猜测。
- 比较报告必须同时展示原始 baseline 和候选报告的路径、口径及逐项差值。发现历史报告与结论冲突时，立即撤回结论并停止继续优化，先完成基准审计。
- 五项主要参考指标固定为：平均最大回撤、盈利股票占比、收益中位数、平均个股胜率、同公式总盈亏比；同时列出回撤中位数和总交易胜率帮助识别平均值失真。
- 任一主要指标退步时，必须明确写出退步指标、幅度和可能原因，不得声称“五项全部提升”或隐去代价。
- 单项退步不自动否决策略，单项提升也不自动证明策略更好。是否“整体更好”必须具体分析：指标的重要性和变化幅度、策略收益结构、同股票配对改善覆盖率、OLD/NEW 与密封集一致性、交易成本、回撤风险、赢家集中度及未来数据检查结果。
- 只有在主要改善明显、退步有限且可解释、跨样本证据一致时，才可以给出“整体更好”的结论，并同时保留退步项。重大风险指标恶化或证据相互冲突时，不得自行替换正式策略，必须交由用户确认取舍。

### 非策略改动

- 非策略改动必须保证回测结果前后一致。
- 验证时要对比改动前后的离线报告核心指标和个股交易结果。
- 若非策略改动导致收益、交易次数、买卖点、回撤等回测结果变化，必须解释原因；无法解释时应视为异常。

### Benchmark 提交格式

- 建立或更新 benchmark，以及修正回测指标口径时，commit message 必须使用统一结构：标题、改动摘要、`Performance (<股票池>)`。
- `Performance` 固定按 `Return / Median / tPF / Win rate / Max drawdown` 的顺序书写，股票池必须写入括号，例如 `Performance (ALL305)`；数值直接取自本轮正式离线报告，保留两位小数，不得凭记忆填写。
- `Max drawdown` 默认指逐日收盘盯市后的平均个股最大回撤；引用未逐日盯市的历史结果时，必须显式标为 `legacy non-MTM`，不能与当前口径直接比较。
- 统一模板如下，不能把其他公式的同名指标混入：

```text
<commit title>

- <change summary>

Performance (ALL305):
- Return: 138.76
- Median: 43.77
- tPF: 1.94
- Win rate: 37.67
- Max drawdown: -48.89
```

### 策略改动

- 原始策略对照永久锁定在 commit `a8dd63a`，不得用后来的候选覆盖其身份。候选必须先在独立 worktree、临时进程或其他不覆盖正式源码的隔离环境中完成验证；未通过前不得把候选留在正式运行路径。
- 实验若在验证前触碰正式源码，必须立即恢复原始行为。候选通过隔离验证并迁入工作区后，必须再用 `codes_250.txt` 从正式入口重跑候选报告；无法与原报告同口径比较时，任务不得结束，也不得让调度器继续使用该版本。
- 所有比较数字必须从本轮实际生成的报告或机器汇总中读取，不得凭记忆引用；报告路径、公式和候选身份必须一起记录。
- 多个删除、替换或参数变化组成候选时，必须验证最终组合版本；单项消融的改善不能相加推断，单项结果也不能代替组合结果。
- 家族归因表只说明某类已成交交易的历史表现，不等于删除该买点后的组合反事实。是否删除必须以完整持仓路径重跑为准，因为入场变化会改变后续持仓、退出、资金占用和再入场机会。
- 策略改动必须证明净收益更高，不能只凭单只股票或单个收益指标改善保留。这里的净收益同时包括回测表现、跨样本泛化、参数稳定性、风险、复杂度和可审计性；大幅删除规则、参数和状态时，允许历史指标总体持平或出现有限且可解释的退步，但必须量化代价，并证明不是用重大回撤恶化或留出集失效换来的。
- 对比指标至少包括：
  - `median`、10% 截尾均值和 `P25/P75`
  - 持有期年化收益中位数
  - `avg_return`（只作为右尾辅助观察，不能单独验收）
  - 日等权策略袖套的 `CAGR / max_drawdown / Sharpe`
  - `tPF`
  - `losers` 数量和比例
  - `max_drawdown`
  - 交易次数变化
  - 前五赢家占全部正收益的比例，以及高收益前排股票是否被明显打坏
- 策略改动后必须运行未来函数检测，并要求 0 失败；检测股票采用“影响驱动”选择，不机械固定为原来的 6 只代表股。
- 先在完整离线基准池上比较旧策略与候选策略，找出买点、卖点、持仓状态、触发原因或本次改动涉及的关键中间状态发生变化的股票，形成受影响清单。
- 原 6 只代表股中只有实际受影响的股票需要参加本轮逐日前缀回放；未受影响者可以跳过。若原 6 只均未受影响，必须改从受影响清单中选择，不能因此跳过未来函数检测。
- 未来函数用例选择最小但完整的真实触发集合：本次新增或修改的每条买入路径、卖出路径和关键状态转换至少覆盖一只；A 股和港股均受影响时必须同时覆盖；不为凑固定数量重复测试等价路径。
- 受影响股票很少时全部检测；数量较多时优先选择信号差异最多、收益变化最大、边界条件或稀有路径被触发的股票，并记录选择依据和未检测范围。
- 若无法可靠确定影响范围，或候选导致收益等指标异常大幅改善，必须扩大到原代表集、更多受影响股票或全量检测，不能以节省时间为由缩小验证。
- 如果某个策略的引入导致收益、回撤、胜率或其他关键指标大幅改善，第一反应必须是排查未来函数、未来数据、数据泄漏和不现实成交价，不能先把提升归因于策略有效。
- 上述异常提升必须做有针对性的逐日前缀回放：先遮住完整历史数据，再从第一行开始一行一行放开；每个时点只允许使用当时已出现的数据。
- 逐日前缀结果必须与完整历史计算在同一时点的买点、卖点、持仓状态、触发原因和关键中间状态逐行比对，要求 0 差异。
- 必须核对信号确认时点和成交时点。项目假设在尾盘最后一分钟形成决策并成交，日线回测统一以当日 `close` 作为该时点的近似成交价。
- 当日 `close` 只是尾盘价格代理，不得解释成“收盘后看到完整结果再按已经过去的收盘价成交”；逐日前缀测试只能证明没有读取下一根 K 线，无法消除分钟级近似误差。
- 任何依赖全样本极值、居中窗口、负向 `shift`、回填未来值、事后分类标签或未来更新外部数据的逻辑，均不得进入策略。
- 如果验证结果的综合代价大于复杂度与泛化收益，或副作用过大，应完整回退，不保留半套实验；不得把“有单项下降”机械等同于失败，也不得把“代码更少”机械等同于成功。

### 防过拟合

- 不要为了单一股票做过度优化。
- 不要硬编码股票代码、股票名单或针对某只股票的特殊规则。
- 不要只用一个样本证明策略有效；至少要检查同类股票、代表性股票、高收益前排股票和 losers 变化。
- 新增买点、卖点、路由或风控时，必须同时考虑入场、持有、退出和风险控制的闭环。
- 过拟合既包括策略家族堆叠，也包括参数过多、参数过细，以及策略开关与参数组合形成的隐性自由度；复杂度审计必须同时统计规则、状态、开关和有效参数。
- 参数调整要能解释其市场结构含义，不能只因为某次扫描数字更好就保留。小数精度、狭窄上下界或只在单点达到峰值的参数，默认按样本记忆处理。
- 参数候选必须使用有市场含义的粗粒度数值，并在相邻参数上做敏感性验证；OLD128、NEW122 和真正未揭示的冻结留出集上应呈现方向一致的稳定平台，不能用同一股票池选出单点最优后再把该池当验证集。
- 删除策略时必须连同其专用参数、开关、状态和退出旁路一起评估。删除后收益下降不自动说明该策略有效，也可能是原参数与原策略共同拟合了开发样本。
- 如果删除大量代码、参数和状态后，关键指标总体持平或仅有有限且可解释的退步，同时留出集、回撤、赢家集中度或参数稳定性改善，可以把复杂度下降作为明确收益接受该代价；必须完整披露退步项和取舍依据。
- 在两个效果接近的候选之间，优先选择规则更少、参数更少、参数邻域更平坦、跨分组方向更一致的版本，不为追回开发集中的小幅损失新增补丁。
- `docs/strategy-blacklist.md` 中的策略、路由方式和优化手法不得换名、换开关或藏进组合条件重新引入；只有用户明确要求重新研究时才能解除。原始基线暂时仍包含其中的历史实现，这表示“受保护、待替换的遗留基线”，不表示这些方法重新获准用于新增优化。

### 评估框架的边界

- 本项目的评估框架是“单股独立、满仓或空仓、按个股统计”。它不是中性的：它剥离了横截面选股和仓位管理。禁止在这个框架里测完外部策略后，直接得出“外部策略更差”的结论。海龟、Clenow、Wilcox-Crittenden 一类的收益来自在数百只股票中挑选少数几只并做风险预算，去掉这两者再比较，比较的是框架而不是策略。
- 判定外部策略优劣，必须在与其原始设计一致的口径下复现：组合层面、持仓数上限、仓位管理、真实费用，并报告 CAGR、最大回撤、Sharpe、Calmar 和换手成本。做不到就如实标注“未在原口径下验证”，不得给出优劣结论。
- 同公式盈亏比使用交易收益率的加总而不是资金复利，胜率不反映盈亏幅度，单股收益中位数也不反映组合资金能否被充分投放；三者都不得单独作为策略优劣的判据。
- 任何策略改动都必须同时报告平均收益、分位数、右尾、资金投入比例和组合结果。平均收益下降时要定位变化来自左尾、主体还是右尾，不能未经归因直接写成“用右尾换稳定性”。

### 先诊断后改进

- 提出任何改进方案前，必须先完成策略性质诊断，至少包括：逐股与买入持有的对比（超额中位数、超额均值、跑赢股票比例）、按买入持有分位的超额分层、总收益归因（哪一层股票贡献了收益）、强势股捕获率、以及删除核心信号后的消融。
- 诊断结论决定改进方向。若诊断显示策略主要保护弱势股、同时截断强势股，应优先研究收益来源和持仓结构；不得在缺少反事实全路径证据时继续堆叠入场过滤器。
- 同类候选在开发池或池外反复失败后应停止该方向，复核假设和评估框架；不得把一次候选扫描观察到的幅度写成普遍天花板或必然规律。
- 参数微调与增加过滤器都属于本文件“防过拟合”一节禁止的堆叠模式。改动被判定为“调参”还是“结构性改动”，以是否改变收益来源或仓位结构为准，不以是否新增代码行为准。

### 泛化检验

- 策略改动在开发池上的提升不构成证据。必须在开发池之外、且未参与该候选策略、规则或参数筛选的股票池上复检，报告同口径指标，并说明结论是否一致。
- 池外股票必须按事先声明的流动性和数据质量规则选择，禁止使用候选策略表现参与筛选；同时记录数据源、行业风格、存续偏差和时间覆盖。
- 若某项改动只在开发池成立、在池外消失，必须先按过拟合、数据源差异和样本风格漂移分别排查。在归因完成前不得声称改善；重大风险恶化时先回退，证据冲突时交由用户确认。
- 同期不同股票只能检验横截面泛化，不能代替跨时期验证。重大策略或参数变化还必须使用未参与开发的时间窗口、滚动走前或其他时间外检验。

### 已犯错误与检验清单

本节逐条记录实际踩过的坑，目的是不再重复。新踩到的坑必须补写进来。

候选实现：

- 不得把新的仓位或出场逻辑嫁接在原策略已生成的 `entry_signal` / `exit_signal` 上。这两个标志只在原策略自身持仓期间产生。候选一旦保留了原策略已经清掉的残余仓位，此后不会再收到任何出场标志，残余仓位被永久遗弃，回测结果完全失真。改动持仓结构必须重写完整状态机，从原始的 entry_condition / exit_condition 出发。
- 任何改动仓位结构的候选，必须先做退化检验：把参数设成等价于原策略的取值（满仓入场、不分批出、不加附加规则），验证与基线逐笔一致。不一致说明实现有错，而不是效果不好，此时任何指标都不能解读。
- 隔离环境的基类必须与当前正式策略保持同步。基类版本漂移会让对照组和候选组的入场条件不同，比较无效。评估工具每次运行都要强制自检；自检失败时所有候选结果作废。

信号设计：

- 不得只凭浮盈把持仓定义为“趋势确认”或直接加仓。浮盈与追高约束可能形成自相矛盾的规则；必须先证明它对后续收益有独立预测力，并重跑完整仓位状态机。
- 判断“主升浪”“启动”一类状态，必须先在持仓样本上做实证，用当日可得特征对未来收益分组，报告相对基础概率的提升幅度；不得凭经验直接写入规则。
- 特征的粗糙代理可能给出方向完全相反的结论。用区间宽度代理“横盘整理”，得到的结论与经典形态理论相反，这种情况必须先怀疑代理实现，再怀疑理论本身。

估计与验证：

- 静态叠加（把新条件叠加在已成交交易上做统计）只能用于候选排序，不得用于判定取舍。它忽略路径依赖，结果可能与全路径重跑明显偏离。
- 引入任何预训练模型前，必须先确认其训练数据的截止日期，并以主要来源（论文、模型卡、仓库）为准。截止日晚于回测起点的模型，用于该窗口的回测无效，只能用于向前的实盘前瞻测试。
- 自选股票池计算出的市场级指标（宽度、情绪等）含有选择偏差：成分股是事后确定的幸存者，可能产生比全市场更乐观的状态。必须用与股票池无关的外部序列（指数点位等）复核，并报告两者差异。

### 验收证据

- 五项主要参考指标继续按本文件既有要求披露，但不能用“改善项数量”投票决定策略。最终判断还必须结合逐股买入持有超额、组合 CAGR/回撤/Sharpe、资金投入比例、滑点敏感性、赢家集中度、池外结果和时间外结果。
- 买入持有、同波动率基准、置信区间或显著性检验只有在公式、再平衡方式、波动率估计窗口和相关性处理均明确且可复现时才能作为正式证据。禁止用全样本事后波动率缩放构造看似同风险的未来数据基准。
- 候选试验的相关性、回归和统计结论必须保存候选明细、脚本、样本量及机器输出。只有汇总数字而没有可复现证据链时，不得据此改写验收标准。

### 组合口径必须标注

同一份信号在不同资金口径下会给出不同风险画像，必须显式标注口径，并同时给出平均资金投入比例：

- B 口径：起点等额分配全部单股 sleeve，之后不再平衡；正式报告使用该口径。
- A 口径：每日将各单股 sleeve 收益等权聚合，近似每日再平衡；研究 harness 使用该口径。
- C 口径：一个受持仓数上限和排序规则约束的真实资金池；仅组合研究工具使用。

C 口径只能在真实 `entry_signal` 当日参与选股；未获分配的信号必须过期，不能在数日后因腾出仓位而补买。被选中的持仓必须使用与该次真实入场配对的退出状态，期末必须计入平仓费用。组合引擎未通过这些退化与回归测试前，其结果不得进入策略验收。

### 泛化检验的强制条款

- `stock_trading_advisor/data/oos_data/` 的 230 只股票已在 2026-09-08 用于否决 `ma60_12` 候选，因此它是“已揭示外部回归池”，不再是未见验收池。可以用它检查已知退化，禁止在其上选择参数、枚举规则或再次声称最终池外通过。
- 该池使用 Yahoo 重建复权数据，且行业风格、存续条件和当前流动性筛选均与开发池不同。引用时必须同时标明这些限制；不能把原始收益差全部归因于过拟合，也不能忽略其中出现的候选退化。
- 后续需要独立验收时，必须另建未揭示股票池或预先锁定时间外窗口。在结果揭示前固化代码、参数、股票清单、数据哈希、指标公式和接受条件；揭示后无论结果好坏都不得继续在该集合上调参。
- 开发池和曾经的 HK55 留出集都已被反复查看，只能作为开发/回归证据。任何已揭示留出集给出否定结果时必须停止并复核，不得继续宣称跨样本改善。

### 多因子研究的设计与已确认结论（2026-09-09）

研究设计固定为三段，任何因子或因子组合都必须按此顺序验证，不得跳级：

- 挖掘：开发池 `2020-01`~`2023-12`，可反复查看；
- 验证：开发池 `2024-01`~`2026-09`，每个想法只看一次；
- 验收：冻结的 230 只池外股票，只对定型方案运行一次。

判定标准是**符号一致性**优先于幅度：一个因子只有在挖掘期和验证期方向相同才可继续，幅度大但符号翻转的一律按噪声处理。组合排序必须打赢同样信号下的随机排序，且随机基准要用不少于 20 个种子给出标准差，差距以标准差计。

工具：`research/factors.py`（24 个价量因子，每个都注明经济含义与方向假设）、`research/factor_eval.py`（分池分期的 IC / ICIR / 分位差）、`research/panel.py`（横截面面板）、`research/portfolio.py`（组合层评估，支持 `--rank 因子名` 与 `combo:f1+f2`，以及 `--start/--end/--market` 分段）。

已确认的结论，后续研究直接引用，不必重测：

- 横截面上，趋势类因子在本股票池是**负向**的（距 MA60 距离 `-5.57%`、趋势斜率 `-4.29%`、Clenow 趋势质量 `-3.88%`），反转与低波类为正（短期反转 `+4.74%`、负偏度 `+4.65%`、低换手 `+6.36%`）。**买过去涨得多的股票在这个市场是亏钱的**，这解释了横截面动量排序在池外反向的原因。
- 时序上同样如此，且幅度更大：一只股票相对自身历史处于趋势强、接近 250 日高点、区间位置高的状态时，未来 40 根 K 线的表现更差（时序 IC `-7%` 到 `-14%`，两期同号）。
- 单因子的方向随时期切换：动量排序在挖掘期比随机差 `5.99pp`，在验证期比随机好 `12.41pp`（`+2.13` 标准差）。**等权因子组合在两期都稳定高出随机约 `0.8` 个标准差**——叠加因子的价值是降低时期依赖，不是抬高峰值。
- 因子的横截面 IC 不能直接换算为组合收益：策略候选池已被信号过滤，因子在该子集上的区分度显著低于全市场。判断一个因子有没有用，必须在候选池内重测。

### 两类改动必须分开评估

- 作用于**组合层**（买哪一只、分配多少资金）的改动，不改变收益中位数、同公式盈亏比和平均胜率这三项单股指标。若目标以这三项衡量，组合层改动无论多有效都无法达标，必须先与用户确认口径。
- 作用于**单股时序**（何时买、何时卖）的改动才会移动上述三项指标。

### 策略对比的样本可比性（2026-09-09 的错误，必须反复对照）

当日犯下的错误：把趋势策略的成交按“状态”分组、把均值回归策略的成交也按同样的状态分组，然后横向比较两者的同公式盈亏比，并据此得出“震荡状态下趋势策略更好、均值回归无效”的结论。这个结论是无效的，三条原因每一条都足以单独否定它。

- **条件于信号的选择偏差。** 每个策略只在自己发出信号的日子入场，两组成交来自完全不同的时点集合。比较“A 在 A 选的日子的表现”与“B 在 B 选的日子的表现”，得到的差异主要来自样本差异，而不是策略优劣。**比较两个策略必须在同一批时点、同一批标的上进行**：要么在同一状态的全部交易日上分别运行完整路径并比较组合结果，要么固定同一批候选日再比较。
- **状态变量被入场逻辑污染。** 当时用过去 20 根 K 线的路径效率定义“震荡”，而该策略本身就在回调之后买入，因此它的入场日天然落在低路径效率区间（1499 笔落在“震荡”组，仅 573 笔落在“趋势”组）。这测量的是“入场前刚回调过”，不是“市场处于震荡”。**状态判定必须与被评估策略的信号相互独立，并且要先验证状态分组在全体交易日上的分布，而不是只在成交样本上**。
- **对荒谬结果没有执行既有纪律。** “趋势策略在趋势状态下表现最差”这种结论本身就应立即触发方法审查。本文件已经规定指标大幅异常时第一反应是排查未来函数与数据问题；该纪律同样适用于任何与机制常识相悖的结果——**先怀疑自己的度量，再怀疑世界**。

附带的度量纪律：同公式盈亏比在成交笔数差异很大的两组之间不可直接比较；分组比较必须同时给出每组笔数，笔数相差数倍时该指标不作为判据。

### 2026-09-09 深度调查的结论与新增的坑

**状态切换（震荡→高抛低吸 / 启动→买入持有）已被证伪，不要重做。** 十四个专家、两轮对抗验证。关键否证：

- 一个不做任何状态判断、零额外换手的固定分账（约 `26%` 资金一直持有，`74%` 跑趋势策略），在两期都在 Sharpe、最大回撤、收益中位数上赢过整套切换系统（验证期 Sharpe `1.31` 对 `1.24`，组合回撤 `-9.50%` 对 `-13.35%`）。切换机器没有产出任何静态杠杆买不到的东西。
- 风险调整后的择时技能在验证期为 `+0.00`（z=0.01）。收益端择时技能 `+1.26pp` 只有 `1.67` 个标准差，换一种零假设打乱方式（rotate）只剩 `0.52` 个——对打乱方式敏感即不成立。
- 切换系统的组合最大回撤**比随机切换标签更差**，七个分组全负。
- 对买入持有的优势符号翻转（`+18.61pp` → `-8.94pp`）。
- 优势在每边 `10bp` 滑点即翻负，而买入持有几乎不受滑点影响。
- 安慰剂检验通过：把路由反向会被自身零假设打穿 `3`~`7` 个标准差，说明震荡分数确实携带方向信息。但那是“何时做多更划算”的信息，在单股满仓框架里只能换成 beta，换不成 alpha。它的正确位置是组合层仓位管理。

推翻上述否定所需的证据（**预先写死，事后不得修改**）：同平均仓位下 Sharpe 与组合最大回撤同时优于静态混合且两期同号、七组中至少六组同号；择时技能在验证期达到 `2` 个标准差且 runshuffle 与 rotate 两种零假设都成立；组合最大回撤不差于随机标签；`20bp` 滑点下仍保住优势；并在未揭示的股票池或事先锁定的时间外窗口复检。

**新增的坑：**

- **“震荡”不是单一概念。** 七个常用震荡指标彼此几乎不相关（多数 `|ρ| < 0.3`，Hurst 与路径效率仅 `-0.03`），它们在测七件不同的事。用单一指标代表震荡必然以偏概全。
- **状态的持续性几乎完全由窗口长度决定。** 同一指标在 `20/60/120/250` 根窗口下的中位持续期差数倍（区间停留 `7/18/31/48` 根）。因此不得写死窗口后再讨论“这个状态能不能用”；必须多尺度并列，或按个股自身节奏自适应。
- **不同尺度测的是不同的东西。** `20` 根与 `250` 根版本的相关系数普遍低于 `0.36`（路径效率 `0.075`、均线斜率 `0.047`），短期震荡与长期震荡可以同时成立，尺度分歧本身就是信息。
- **路径效率不具备状态属性。** 任何尺度下中位持续期都只有 `2`~`3` 根，不能用作切换依据。
- **均值回归效应真实但极弱。** 方差比 `VR(20)` 中位 `0.900`、低于 `1` 的占 `64.3%`（随机游走应为 `50%`），但统计显著的仅 `0.1%`。因此该家族的成败取决于交易成本与独立下注次数，而非入场规则的精巧程度。
- **没有任何检测器能预测未来的自相关性。** 也就是说“接下来均值回归会有效”在本市场事前不可知，这是状态切换前提的根本缺陷。
- **均值回归应在趋势状态中做，不是在震荡状态中做。** 限制在震荡档盈亏比 `1.03`，限制在趋势档 `1.12` 且回撤由 `-25%` 降至 `-13%`。与直觉相反，但两期一致。
- **调研深度直接决定结论。** 同一框架同一费率下，随手写的均值回归规则（跌破 `MA20` 一个半 `ATR`）收益中位数 `-2.90%`、Sharpe `0.09`；调研整个家族后找到的 IBS 规则为 `+21.52%`、Sharpe `0.99`。差别来自把家族查清楚，不是调参。**因此“测过一个版本就否定一整类方法”是无效结论。**
- **Hurst 门的真实性质（已实测，不要照字面理解）。** `_calculate_hurst_exponent` 对纯随机游走返回 `0.6531 ± 0.0460`，而阈值 `0.65` 恰在该零分布中位数 `0.6539` 上，故在真实股票上约 `50%` 触发、挡掉 `32.4%` 的折价买点。但估计器本身有区分力（AR(1) `+0.4` → `0.76`，`-0.4` → `0.54`）。删除该门或把阈值移到零分布上尾（`0.712`/`0.745`），三种改法在快筛集上**全部劣于现状**（均值 `165.92` → `149.93`~`158.58`，盈亏比 `1.90` → `1.83`~`1.85`）。结论：该门的实际作用是给低信噪比的折价路由随机限流，**保持现状，且不得据此宣称 Hurst 有预测力**。

### 快筛集只能筛选，不能下结论（2026-09-09 的错误）

`research/fast_set.txt` 的 30 只分层代表股用于**快速淘汰**候选，绝不能用于**确立机制**。当日的错误：在 30 只上按 MA120 斜率给 IBS 成交分组，得到“陡峭上行档每笔均值 `+1.06%`、走平档 `+0.22%`，相差八倍”，据此宣称“IBS 的本质是强上升趋势中的回调买入”。

用全部 305 只重算，该关系消失：走平档盈亏比 `1.36`、缓升 `1.46`、陡升 `1.23`——陡峭档反而最低。在干净的 230 只上方向完全相反：`1.27 / 1.25 / 1.02`，陡峭档每笔均值仅 `+0.06%`。按市场和时期拆成四格，符号无一稳定（A 股验证期 `-1.59pp`、挖掘期 `+0.05pp`；港股 `-0.10pp` 与 `+0.57pp`）。

**规则：**

- 30 只样本可以否定一个候选（表现极差时），但**任何正向结论都必须在全池复算后才能陈述**，分组结论尤其如此——分组会把 30 只再切成三份，每份仅十只股票的交易。
- 陈述任何“机制”前，必须给出该关系在两个市场 × 两个时期四个格子中的符号，四格符号不一致即不成立。
- 本项目已两次因分组方法出错得到反直觉结论：一次是用被策略信号污染的状态变量分组，一次是在过小的样本上分组。两者的共同点是**先看到差异、后编造机制**。正确顺序是先声明机制假设、再设计能证伪它的检验。

### 开发池失效的根本原因（2026-09-09 溯因）

三个候选连续在池外失败，原因已定位，且不是笼统的“过拟合”。

`backtest_benchmark/manifest.json` 的 `path_type` 显示，305 只中有 **250 只按整段历史的价格路径被贴了标签**（`sideways_1y` 124、`growth` 54、`mixed` 46、`drawdown_sample` 26），只有 55 只（港股留出集）不含表现输入。**标签本身就是按“这只股票后来走成什么样”划分的。**

以质量门为例，逐子集测量：

| 子集 | 只数 | 基线中位数 | 加门后 | 说明 |
|---|---|---|---|---|
| `growth` | 54 | 185.04% | 217.58% | 涨幅最大 |
| `sideways_1y` | 124 | 26.84% | 33.96% | |
| `drawdown_sample` | 26 | -14.75% | -10.64% | |
| 不含表现选择 | 55 | 22.31% | 21.99% | **唯一下降的子集** |

市场混淆已排除：那 55 只全是港股，但在两个干净池上，**两个市场同时为负**（干净 230：A 股 `-4.91pp`、港股 `-1.72pp`；干净 292：A 股 `-8.03pp`、港股 `+0.07pp`），而在 305 池上两个市场同时为正（A 股 `+2.38pp`、港股 `+4.91pp`）。所以不是市场差异，是股票池差异。

**机制：** 被事后归为 `growth` 的股票，其“低参与度”时段恰是上涨前的蓄势期；被归为 `sideways_1y` 的股票，其“高参与度”时段恰是少数几次真实突破。**由于分组依据就是最终走势，任何能区分时段的规则都会在这些股票上自动显得有效，即便它的真实择时能力为零。**

**由此得出的操作规则：**

- 305 池**不得用于开发任何择时类改动**（入场过滤、状态门、信号打分、仓位择时）。它对这类改动有系统性放大作用，实测把收益中位数放大约五倍（`43.77%` 对干净池 `8.24%`）、盈利股票占比由 `54.79%` 虚增到 `73.77%`。
- 它仍可用于：非策略改动的一致性回归、费用与口径核对、以及作为“有偏参照”量化偏差幅度。
- 开发应在 `pool_b_dev`（292 只，仅按流动性与数据质量分层抽样）进行，验收用 `pool_b_acc`（292 只，同分布，未被读取）。
- 判断一个候选是否属于“被池子放大”，最快的诊断是**按 `path_type` 分子集重测**：若效果集中在带标签子集、而在 55 只干净子集上消失或反向，即可判定。

### 股票池状态与已知偏差（2026-09-10 更新）

| 池 | 只数 | 用途 | 状态 |
|---|---|---|---|
| `backtest_data`（305） | 305 | 回归一致性、口径核对 | **禁止开发择时类改动**（250 只带事后路径标签） |
| `oos_data`（230） | 230 | 二次回归 | 已揭示 3 次 |
| `pool_b_dev`（292） | 292 | **开发** | 干净，可反复使用 |
| `pool_b_acc`（292） | 292 | 验收 | **2026-09-10 已揭示一次**（候选 `tk_hold_ex_mom`，规则 120/250/0.90）；此后只能做回归检查，不得再据其调参或选候选 |

**pool_b 与 oos230 共有的构造偏差（已量化，无法在池内消除）：**

- 流动性门槛按 **2026-09-09 当前快照**施加，不是逐时点的。一只 2020 年很薄、如今仍在池中的股票，从机制上就是价格涨上来的股票：换手增长与全期收益的 Spearman 相关 `+0.740`；2020-07 有 39.0% 的池内股票低于自己的门槛，期末只剩 5.8%。任何向早期低流动性/低价股倾斜的规则都会被这条通道抬高（一个纯粹按“持有最不流动的 100 只”的规则得到 +310% 对买入持有 +109%，切断该通道后变成 +55%，反输 73 点）。
- 当前上市快照，**不含退市股**。这会系统性美化“卖出拉伸、跌回再买”一类的反动量规则——被永久性下跌杀死的正是这类规则，而池里一只这样的股票都没有。
- 数据源为 Yahoo（305 池为 akshare），逐日 K 线不逐位可比，只能在同池内比较。

### 同收盘成交的市场结构陷阱（2026-09-10，两个干净池独立复现）

按市场分解全部交易日的日收益（bp/日）：

| 池 | 市场 | 隔夜 | 盘中 | 收盘到收盘 |
|---|---|---|---|---|
| pool_b_dev | A 股 | **-6.0** | **+12.6** | +6.2 |
| pool_b_dev | 港股 | **+12.9** | **-5.9** | +6.5 |
| oos230 | A 股 | -5.3 | +11.3 | +5.7 |
| oos230 | 港股 | +15.1 | -7.6 | +6.9 |

两个市场的日漂移相同，但**A 股全部赚在盘中、港股全部赚在隔夜**。后果：回测在收盘决策并同收盘成交，对港股的短持有规则来说，入场当天的隔夜跳空（IBS 规则约 +38 bp/笔）被算进了收益，而这一段处在买卖价差水平，**不可收割**。IBS 在港股“显著优于”A 股（tPF 1.51 对 1.07）的全部差距都来自这一段：按次日开盘重定价后两市中位数一样（7.17% 对 7.30%），6/6 池×窗口、14/14 年×市场符号一致。**规则：任何港股短持有规则的结论，必须附带次日开盘成交的复算；同收盘成交的港股回测结果不能单独采信。** A 股相反，同收盘入场吃到的是负的隔夜，回测偏保守。

A 股盘中正、隔夜负的结构本身也被确认与涨跌停制度相关（触及涨跌停的日子造成正的一阶自相关；用同等幅度的非涨跌停大波动日做安慰剂，自相关反而上升）。

### 干净池上唯一通过验收的候选，以及它为什么仍不成立（2026-09-10）

候选 `hem` / `tk_hold_ex_mom`：120 根动量在自身 250 根历史中的分位 > 0.90 则空仓，否则满仓；同收盘成交；单参数；平均仓位 83–90%。定义在 `research/candidates_local.py` 与 `research/candidates_trendkeep.py`。

**它通过的东西（全部独立复现）：** 从文字规格重实现与注册代码逐位一致（827 只、133 万根 K 线）；22,593 个前缀点 0 差异；出场延迟一根仍四格为正；A 股在两个干净池 × 两个时期四格同号（配对中位超额 +7.7 到 +11.7pp，跑赢 61–71%）；90 格参数曲面 63 格四格全正、无一格四格全负，选中格在内部而非悬崖；305 污染池对它是**惩罚**而非放大（growth 子集 -63pp）；**验收池**（pool_b_acc）全期中位数 44.26% 对 33.41%、回撤 -60.23% 对 -63.98%、两期同号。

**它为什么仍不成立——三条，每条都是测出来的：**

1. **各股出场高度同步，有效样本极小。** 出场掩码的股票间相关 0.10–0.21，单日最多 70% 的股票同时空仓，有效独立掩码只有 4.7–10.1 个（不是 200/150）。用**保留同步性的零假设**（所有股票施加同一日历平移，仓位、段长、横截面结构全部不变，只有时点错误），z 从逐股零假设的 5–8 掉到 **+1.07 到 +1.75**，四格单侧 p 0.03–0.09，联合 p 0.012–0.017。
2. **"两个独立池"在日历上不独立。** 在该零假设下两个池的结果相关 +0.90/+0.95，两个时期相关 +0.76——**四格约等于 1.5 次检验**，不是四次。验收池 A 股验证期 +4.53pp、p = 0.163，与此一致。
3. **收益来自两次市场级事件。** 2022 年贡献挖掘期超额的 70–76%，2024Q4 贡献验证期的 65–71%；2020–2021 完全休眠（超额恒为 0）。各股自身最差 1% 的回避日贡献 64–71% 的挖掘期超额、超过 100% 的验证期超额；仅 2024-10-09 一天（200 只中 111 只同时空仓）就把验证期从 +9.49 拉到 +1.38。剔除单一事件后残差 z +0.5 到 +0.95，p 0.14–0.19。

**它的真实性质：** 一条 A 股的市场级崩盘回避规则，被重复到 200 只股票上。对单票持有者，复利年化从 4.5–4.8% 提到 6.3–6.8%（来自波动下降、复利拖累减少），对分散组合 CAGR 不变、波动低约 11%。**不是收益策略。** 而且它的独特成分（自身历史分位）不做功：同日横截面动量分位给出等量或更大的反转，而只有自身分位才能看到的那三分之二触发在挖掘期价值约为零。

**顺带被确证的一个市场事实（可复用）：** A 股处于自身动量前十分位的股票，随后 20/40/60 根的收益比其余股票低 2–4pp，指数中性后仍成立，12/12 池×年×市场格同号，块自举 3/4 格显著。港股无此效应。这是本项目此前测出的"A 股动量 IC 为负"的同一件事。

**两个对抗方的分歧与裁定（必须一起读）：** 第五路裁定方与上面第 1 条持相反意见——它把组合增益拆成"选股成分"（同一天空仓股比在场股跑输 4.7–14.9 bp/日，12/12 池-年为负，mask-swap 零假设 p ≤ 0.0025）与"广度择时成分"（+0.4–0.7pp/年，全部不显著），认为前者是真实的横截面事实、不依赖日历。两种读法都有数字支撑，不矛盾：**横截面反转是真的（A 股拉伸股随后 1–3 个月跑输同期其他股票），但把它做成单票空仓规则后，金额上的超额集中在少数市场级崩盘日**。裁定方给出的部署包络：只做 A 股、≥100 只等权篮子上的降风险覆盖层、T+1 延迟一根 bar 执行、预期相对买入持有 **+0.5 到 +2.4pp/年**、Sharpe +0.06 到 +0.14、往返成本 > 40bp 即失效；验收池验证期加 10bp 滑点后只剩 +2.55pp、胜率 53.5%。**这不是收益引擎，也不满足本项目"大幅提升"的目标；主策略源码未改。**

**方法论上必须记住的四条：**

- 任何有仓位切换的规则，**逐股零假设会把显著性高估 3.5–5.5 倍**。必须补做保留同步性的日历平移零假设，并报告有效独立样本数。
- 股票池在成分上独立，不等于在日历上独立。跨池"重复验证"前先测两池在零假设下的相关。
- 任何回避类规则必须做**逐事件剔除**（leave-one-episode-out）和逐年分解；2020–2021 为 0 而两年撑起全部，就是单一事件。
- **验收池被过早用掉了。** 当时的放行条件（中位数跑赢、回撤不更差、两期同号）在逐股零假设下全部满足，但上面三条检验一条都没做。正确顺序是：日历平移零假设 + 逐事件剔除**先于**验收，验收只给同时通过这两项的候选。

### 策略在一只股票上的成败由什么决定，以及为什么筛选不可行（2026-09-10，逐笔取证）

工具：`research/trade_forensics.py`（逐笔账本 + 进出场标注图 + 状态归因），
`python3 research/trade_forensics.py baseline --pool pool_b_dev --charts 10 --report`。

**恒等式分解**（对数空间精确可加）：`买入持有 = 持仓期赚到的 + 空仓期错过的`。因此策略相对买入持有的超额，一阶上等于**空仓期收益的相反数**。

**实测：空仓期收益与超额的 Spearman 相关 = -0.94 到 -0.96**（两个干净池 × 两个市场，四格全部）。按空仓期收益把股票分四档：

| 空仓期股票表现 | 只数 | 策略中位 | 买入持有中位 | 跑赢买入持有 |
|---|---|---|---|---|
| 跌得最狠 | 73 | +41.6% | -19.4% | **100.0%** |
| 跌 | 73 | +22.1% | +14.3% | 68.5% |
| 涨 | 73 | +7.0% | +51.7% | **0.0%** |
| 涨得最多 | 73 | -16.4% | +119.7% | **0.0%** |

单调、无例外、两池一致。**策略的本质是一个赌注：我空仓时股票会跌。** 买卖点本身没有系统性缺陷——最好与最差的股票，止损都打在 -6% 到 -10%，出场理由分布几乎相同。典型案例：`603979` 买入持有 +823.9%、策略 +19.1%，31 笔交易合计 +52.8pp 而**空仓期踏空 +223.5pp**，30 个空仓段有 23 段是以更高价买回；`002759` 相反，空仓期股票累计 -133.5pp，策略 +1114.2% 对买入持有 +80.4%。

**但这个决定性的量没有任何持续性，因此无法筛选、无法切换：**

- 用挖掘期（2020–2023）的空仓期收益预测验证期（2024–2026）：Spearman **+0.027 / -0.015**，四分位表无单调性。
- 把每只股票切成连续不重叠窗口，检验相邻窗口的边际是否相关：h = 20 / 60 / 120 / 250 根，Spearman 全部落在 **-0.096 到 +0.094**，且**多数为负**；同号概率在 h=20 时只有 **40.8–42.7%，低于抛硬币**。
- 直接检验切换规则"在策略最近有效的股票上继续使用它"：**16 个格子里 14 个方向为负**，过去有效的股票下一窗口反而差 **-1.17 到 -2.59pp**。该规则不是无效，是有害。

**结论（限定范围，2026-09-10 用户指出后收窄）：** 上述检验只证明了一件事——**一只股票上策略过去的盈亏，不能预测它未来的盈亏**。因此"按策略历史表现给股票打适用性分""在最近有效的股票上继续用"这两类做法无效且有害。

**它没有证明的（我当时越界了）：** 它没有证明"震荡与主升浪不可识别"。用户的反驳成立：

- 被检验的量是"边际"，即两个收益相减的差值，噪声极大，自相关低几乎是构造性的必然；
- 窗口是从第 0 根开始的固定不重叠网格，主升浪不会按这个网格对齐；
- **"派生盈亏指标不持续"与"状态不可检测"是两回事。** 检测应当直接以状态为标签（例如"此后 60 根内涨幅 ≥30%"）做有基准率、有精确率/召回率的检测评估，而不是测盈亏的自相关。

**按正确形式重测的结果（2026-09-10）：主升浪可以被识别，用户的直觉成立。**

把问题改成有基准率的检测问题——标签为"此后 60 根内上涨 ≥30%"，特征全部因果，阈值仅在 pool_b_dev 2020–2023 拟合后原样外推：

| 单特征（前十分位） | 训练提升度 | 样本外提升度 | oos 池 |
|---|---|---|---|
| 跳空频率 | 1.74 | 1.24 | 1.44 |
| 20/60 日涨幅 | 1.52 | 1.27 | 1.28 |
| MA120 斜率 | 1.45 | 1.26 | 1.26 |
| MA250 斜率 | 1.39 | 1.19 | 1.37 |
| 相对成交量 | 1.35 | 1.21 | 1.25 |

综合打分前十分位在 **8 个格子全部为正**，提升度 1.20–1.73（基准率 12.2–22.5%，命中率 19.9–31.6%）。

**强度与保留：** 命中率约从 18% 提到 23%，**召回仅 13%**——前十分位只覆盖八分之一的主升浪，是真实但不强的信号。综合打分使用了全样本百分位归一化，不完全因果；单特征列用训练期阈值外推，是干净的。**两个结论并存且不矛盾：策略在某股上的历史盈亏不可预测未来盈亏；但"正在起飞"可以从形态中以约 1.3 的提升度读出。**

**教训：** 一个零结果只能否定它实际检验的那个命题。把"我测的量没有持续性"扩写成"这类现象不存在"，是本项目第四次分组/推论层面的越界（前三次：被信号污染的状态变量分组、30 只样本下机制结论、当期状态归因未滞后）。**陈述否定结论时必须写明它否定的确切命题，以及它没有否定什么。**

### 决定性分解：策略的全部劣势在"没持有的那些天"（2026-09-10，五路场景地图 + 14 路对抗验证）

面板 848,441 个股票-日、522 只股票（pool_b_dev 292 + oos 230），`pos_t = buy_signal.shift(1)`，毛值，所有状态滞后 1 日且与信号无关。基准（毛 bp/日）：买入持有 **+6.20**、趋势策略 **+2.92**（仓位 41.0%）、dib_ma120 +1.62（14.4%）、hem +5.81（86.9%）。**每持仓日**：趋势 **7.12**、dip 11.21、hem 6.68，买入持有 6.20。

**恒等式分解 `diff = -(1-e)·B + e·(pu-B)`：** 全样本趋势策略输给买入持有 **-3.35 bp/日（8/8）** = **仓位缺口 -4.01（8/8）** + **择时/选股 +0.66（0/8）**。

**即：策略在"持有的那些天"并不差（7.12 > 6.20），全部劣势来自"没持有的那些天"。** 逐格重复：风暴格 total -9.14 = drag -9.31(8/8) + timing +0.17(2/8)；高波格 -7.16 = drag -6.58(8/8) + timing -0.58(2/8)。事先登记的证伪条件（若是"信号在这些状态里变差"则 timing 必须 8/8 为负）**未出现**。

**因此"场景→策略"的映射本质是"场景→仓位"的映射**，不是"什么场景换什么策略"。这统一解释了卖飞、踏空、以及本周全部切换/筛选的失败。

**场景地图的 8/8 结论（毛 bp/日）：**

| 状态（滞后1日） | 占比 | 买入持有 | 趋势 | dip | 判定 |
|---|---|---|---|---|---|
| 市场风暴（横截面中位波动前 1/3） | 32.5% | **+13.81** | +5.64 | +2.94 | **B 最好 8/8（+14.93）；T -9.14、D -11.73、H -2.52 均 8/8 最差** |
| 自身高波 × 市场风暴 | 18.3% | **+19.29** | +6.71 | +3.51 | **B +21.42 8/8；T -14.33、D -17.38 8/8** |
| 高波 × 破 MA120 | 12.4% | **+17.40** | +2.83（仓位 22.7%） | +0.37 | **最恶劣格：T -15.70、D -17.79 8/8** |
| 距 250 日高点 < -25% | 39.6% | +9.39 | +3.22（24.5%） | +1.36 | T -6.32、D -8.22 8/8 |
| 市场平静 | 37.2% | **-1.76** | +0.69 | +0.63 | 原始无赢家；**指数中性后 T 最好 8/8（+1.25）** |
| 自身高波 × 市场平静 | 6.2% | -4.72 | -0.45 | -1.30 | 完全打平，无任何 8/8 |
| 路径效率 高/中/低 | 各 ~33% | +6.4/+6.1/+5.9 | 三档全 8/8 落后 | — | **"趋势状态里趋势策略更好"不成立** |

**关键澄清：** 买入持有在高波动格赢，不是因为"高波动有溢价"，而是因为**别的策略此时仓位最低**（趋势 22–41%、dip 3–17%）。而且"是市场不是个股"：扣掉同日同池指数收益后，个股自身高波溢价降到 **2/8（-0.42）**。

**筛选（第 4 问的准确回答）：不可行。** 5,740 个 120 日窗口，均值超额 -3.40pp，仅 41.5% 为正。窗口**起点**的全部因果特征与窗口超额的 Spearman 相关：波动分位 +0.023(2/8)、回撤 -0.011(0/8)、动量 -0.016(0/8)、均线排列 -0.014(4/8)、市场波动态 -0.070(6/8)——**没有一个达到 8/8，最大 |rho| = 0.070**。窗口成交额筛选看似 8/8 +2.48pp，但**同尺寸随机筛选给 +2.12pp**，真实增量仅 +0.36——它"有效"只因为**把策略关掉有效**。同一只股票 002717 相邻两个窗口分别 +88.7pp 与 -145.7pp，仓位都是 0–21%：**"好股票/坏股票"不是稳定属性。**

**最好的补仓规则及其三条限定。** 规则形式 `pos = pos_趋势 + (1-pos_趋势) × mask`。最优为 **R4：自身波动前 1/3 且收盘 < MA120 时把空仓补满**——毛 **+2.05 bp/日（8/8）**、净 +1.91、胜过**完全等仓位的无状态混合** +1.47（8/8）、日历保持零假设 **z=+5.30, p=0.000**、逐季留一 8/8、83% 个股受益。但：

1. **它是市场 beta 不是选股**：五条规则的指数中性增益全部只有 2/8–4/8、+0.05..+0.36 bp/日，扣掉同日同池均值后改进基本消失。
2. **"8/8"在此很便宜**：日历保持零假设下随机 mask 自己达到 8/8 的概率是 **0.125–0.710**（R5 在逐股零假设下达 0.98）。撑住结论的是量级与 z，不是符号一致性。
3. **改进后仍输给满仓**：净水平 3.44–5.09 bp/日，买入持有 6.20。

**唯一不靠加仓且过零假设的正向发现（仅相对口径）：** 市场平静日（37.2%），趋势策略每持仓日跑赢同日同池同市场均值 **+2.91 bp（8/8）**，组合层 +1.25（8/8），日历零假设 z=+2.69 p=0.003，逐年 2020–2026 全正，dip 6/8、hem 2/8（趋势策略专有）。**弱点同时记录**：逐季留一最差 6/8、滞后 5 日降到 6/8、只有 52% 个股为正、前 5% 股票占总和 99%、原始非中性口径 T-B 仅 2/8。**只能用于相对权重/对冲口径，不能宣称"平静期用趋势策略赚绝对收益"。**

### 主升浪捕获的差异：路径效率（8/8），但事前不可知（2026-09-10 逐股逐日分析）

用 305 只全量图表（`research/out/forensics/baseline__dev/charts/`，305 张）与 477,338 行逐日面板（同目录 `daily.csv`）做逐股分析。读图得到的假设——"平滑的主升浪抓得住，暴烈的抓不住"——量化后**方向对但变量错**：决定成败的不是波动幅度，是**路径效率**（净涨幅 ÷ 走过的路程）。

主升浪定义：自 120 根低点起、涨幅 ≥100%、以自峰值回撤 20% 结束。三池共 676 段，**整体中位捕获率仅 0.678**。

| 路径效率 | 段数 | 中位捕获率 | 中位仓位 | 中位涨幅 |
|---|---|---|---|---|
| 低（曲折） | 226 | **0.565** | 54% | +100% |
| 中 | 223 | 0.716 | 60% | +101% |
| 高（平滑） | 227 | **0.797** | 58% | +113% |

**干净池 8/8 全为正**（+0.10 ~ +0.32）。**不是仓位效应**——仓位仅差 4pp（54% vs 58%），而每单位仓位的捕获率是 1.046 对 1.374。对照：按"实现波动率"分桶只有 **5/8**，按"单日涨跌 >7% 的天数占比"只有 **5/8**，按"段内最深回撤"7/8（且方向是回撤越深捕获越好，因为段更长）。

**但它事前不可知，且事前指标符号相反：**

| 预测变量 | 目标 | Spearman | 干净池格子 |
|---|---|---|---|
| 整段效率（事后，参照） | 捕获率 | **+0.214** | **8/8** |
| 起点前 60 根效率 | 捕获率 | **-0.168** | **3/8** |
| 起点前 120 根效率 | 捕获率 | **-0.199** | **3/8** |
| 段内前 20 根效率 | 其余段捕获率 | -0.006 | 6/8 |

同一只股票**上一段主升浪的效率与这一段的相关仅 +0.063**（n=160），对本段捕获率相关 **+0.018**——**平滑性不是股票的稳定属性**。近期走势越平滑，下一波反而捕获更差。

**广谱因果扫描（21 个特征 × 590 段）：在主升浪起点，8 格全同号的特征 0/21。** 事前完全不可预测。

**开始 20 根后，43 个变量中仅 1 个 8/8——`ret20`（本波自身前 20 根涨幅），Spearman +0.190。但它是仓位通道的伪装：** 按 bar 20 时是否已持仓拆开后，空仓组里"开局强"反而**捕获最差**（0.482 对弱开局的 0.600），组内 8 格检验仅 **1/5**；而各强度档在 bar 20 时的在场比例是 7.9% → 42.9% → **73.6%**。效应全部由此产生。

**"开局强却仍空仓"人群（52 段，后续中位再涨 +80%）是按结果选样的产物。** 把触发条件写成纯因果形式（20 根内自 120 根低点涨 ≥X% 且当前空仓），对**所有**满足条件的 bar 触发、以同池无条件 60 根收益为基准：

| 阈值 X | 触发数 | 干净池格子（超额为正） |
|---|---|---|
| 20% | 8,268 | 6/8 |
| 30% | 3,886 | **3/8** |
| 40% | 2,003 | **1/8** |

X=40% 时验证期各格**强烈为负**（60 根中位 -6.3% ~ -13.9%，无条件基准 -0.5%）。同一触发条件同时大量抓到冲高回落，与本市场动量 IC 为负一致。**该线索封闭。**

**这与本项目全部其他发现同形：已发生的结果可以精确解释，将发生的无法预测。**

### 305 只逐股归档（2026-09-10）

| 类型 | 只数 | 中位 B&H | 中位超额 | 仓位 | 踏空率 | 卖飞率 |
|---|---|---|---|---|---|---|
| A 熊股保护 | 92（30.2%） | -41.6% | **+54.9pp** | 34.1% | 66.2% | 29.2% |
| B 涨股跑赢 | 76（24.9%） | +59.4% | +86.5pp | 40.8% | 55.4% | 34.6% |
| C 踏空型 | 15（4.9%） | +41.5% | -26.5pp | 37.9% | **81.1%** | 25.0% |
| **D 卖飞型** | **52（17.0%）** | **+299.0%** | **-137.0pp** | 42.1% | 56.4% | **42.2%** |
| E 磨损型 | 70（23.0%） | +55.0% | -40.5pp | 45.0% | 54.6% | 21.1% |

损失集中在 D 组 52 只（中位涨幅 +299% 的大牛股，平均亏 137pp）。逐股恒等式确认：Spearman(超额, 空仓期收益) = **-0.951**。约 **70% 的个股**其状态内日选择能力为负（MA120 上方 -8.3 bp/日、下方 -15.4 bp/日）。

典型对照：**300502** B&H +4965%、策略 +2052%、亏 2912pp，但其状态内技能是**正的**（+1.6 bp）——纯粹是 43% 仓位在 50 倍股票上不够用（踏空的纯粹形态）。**300394** B&H +3020%、策略 +151%，空仓期股票涨了 **+1142%**；其 2026 年 9 倍主升浪期间高波动阴影几乎连续，绿色持仓带碎成小段。反面 **300274** 策略 +4250% 对 B&H +1020%，靠**一笔 +693%** 持有约 18 个月的平滑上涨，外加避开 2022 年崩盘。

工具：`research/trade_forensics.py`（新增 `--all-charts` 与 `--daily`）。

### 日选择能力为零：按状态分桶后 16/16 格为负（2026-09-10 日线分析）

用因果口径（`pos_t = buy_signal.shift(1)`，度量 `r_t`）比较"持有日"与"空仓日"的次日收益：

| 池 / 市场 / 时期 | 持有日 | 空仓日 | 差 | 仓位 |
|---|---|---|---|---|
| pool_b_dev A股 挖掘 | +4.9 | +5.9 | **-1.0** | 42.1% |
| pool_b_dev A股 验证 | +10.5 | +5.0 | **+5.5** | 40.3% |
| pool_b_dev 港股 挖掘 | +1.9 | +3.6 | **-1.7** | 38.6% |
| pool_b_dev 港股 验证 | +16.1 | +8.9 | **+7.2** | 41.5% |
| oos A股 挖掘 / 验证 | +2.6 / +10.0 | +7.0 / +3.9 | -4.4 / +6.0 | 42.5 / 41.3% |
| oos 港股 挖掘 / 验证 | +3.3 / +18.0 | +3.7 / +7.5 | -0.4 / +10.5 | 38.0 / 42.0% |

表面上验证期出现了择时能力（+5.5 ~ +10.5 bp/日）。**但按股票自身状态（是否在 MA120 上方）分桶后，该优势全部消失并反号，16 个格子无一例外：**

| | 挖掘期 | 验证期 |
|---|---|---|
| pool_b_dev A股 · MA120 上方 / 下方 | -22.3 / -22.2 | **-9.5 / -36.9** |
| pool_b_dev 港股 · MA120 上方 / 下方 | -28.8 / -25.9 | **-12.0 / -51.3** |
| oos A股 · 上方 / 下方 | -25.5 / -22.8 | -11.6 / -34.0 |
| oos 港股 · 上方 / 下方 | -29.1 / -25.8 | -11.5 / -50.8 |

**结论：策略没有日选择能力。** 在同一状态内，它选中持有的日子系统性地差于它放弃的日子（-9.5 ~ -51.3 bp/日，16/16）。

**"2024 年后择时变好"是市场变了，不是策略变了**——纯数据层面（不涉及策略）：MA120 上方天数占比由 42.2%→**49.7%**（A 股）、41.1%→**58.0%**（港股）；港股日均收益由 +2.9 bp 跳到 **+11.9 bp**；年化波动 47.2%→49.4%；AR(1) 与横截面离散度基本不变。**策略在验证期持有得更多的那个状态整体变好了，混合效应造出了择时能力的假象。**

这与游程置换零假设（基线自身分位 22.0/28.0，挖掘期四格 0/0/0/24）互相印证：两种独立测法都说它的时点选择不优于随机重排。**归因必须在状态内做，跨状态的合计会被状态占比的变化污染。**

### 最终裁定：卖飞/踏空无解，且策略自身的择时不优于随机重排（2026-09-10）

对 5 个方向（再入场、状态相关 runner 离场、主升浪检测、替换/组合、闸门放开）共约 50 个候选测完后，**没有一个满足 8/8，也没有一个能与自身配平仓位的零假设区分开。**

**最尖锐的一个数字——游程置换零假设**（在每只股票内部打乱该系统自身的持仓段与空仓段顺序，仓位、笔数、持仓时长分布完全不变，200 seed）：**基线策略自身的中位数只落在零假设的第 22.0 / 28.0 百分位**。即**随机重排它自己的持仓段，多数情况下比它实际的时点更好**。分格更清楚：挖掘期四格分位 **0 / 0 / 0 / 24**，验证期四格 **82 / 67 / 89 / 78**。

配套的独立测量：持仓 bar 的次日收益减全样本均值，**挖掘期 4/4 为负，验证期 4/4 为正**。**策略的全部择时优势集中在 2024 年以后**，2020–2023 段其时点选择是负贡献。

**恒等式确认：** Spearman(超额对数收益, 空仓期收益) = **-1.000 / -0.999 / -1.000 / -1.000**——这就是恒等式本身，"策略 = 赌你空仓时它跌"完全成立。

**组装出的最好系统**（主升浪检测器 runner 袖 + KL 再入场）确实做到了表面指标：Q5 捕获率 0.394→0.616 与 0.428→0.563，空仓错过总额砍掉 57%/56%，**603979 从 +19.1% 提到 +120.8%**。但：

- **组合最大回撤 -8.85→-19.19（+117%）与 -10.42→-19.94（+91%）**；
- 捕获率提升**恰好落在仓位定律的回归线上**（59.2% 仓位预测 0.590/0.559，实测 0.616/0.563，残差 +0.026/+0.004，而梯子系统自身残差散布 ±0.09）；
- 游程置换零假设分位 **61.0 / 63.0（p≈0.38）**，挖掘期四格 3/4/5/89——基线的病根未被治好；
- **镜像股被打坏：002759 从 +1114.2% 跌到 +863.6%（-250pp）。修 603979 的钱是从 002759 身上出的。**
- 8 格 6/8，oos CN-A 验证期为负。

**滑点脆性（必须记住）：每边 20bp 时基线中位数已经是负的（-2.92 | -3.46）。** 整个策略族的边际很薄，任何"多交易 30%"的改动（组装系统笔数 28.7→36.4）都会先被摩擦吃掉。

**唯一"什么都变好"的改动方向与用户诉求相反：** `ma60_12`（追高闸门 18%→12%，即**收紧**而非放开）——中位数 8.24→**10.41** | 7.47→**10.08**，tPF、个股回撤、组合回撤、去尾稳健性全部改善，**仓位反而降到 38.89/39.08**，游程置换分位 **50.5 / 56.0（全场最高）**。但独立的逐股配对检验判它**不成立：8 格仅 1/8 为正**——唯一为正的是 pool_b_dev CN-A 挖掘期 `+1.02pp`，其余七格差值中位数恰为 `+0.00`，改善股票占比仅 28.7%–52.5%（"非配对中位数陷阱"第四次命中）；且 **Q5 捕获率 0.439→0.371、0.464→0.365，确认它让卖飞更严重**。**至此本轮全部候选清零。**

### 捕获率–仓位定律：主升浪的涨幅是买来的，不是赚来的（2026-09-10，决定性结论）

对 50 个系统（从 Darvas、海龟、两袖系统到买入持有，仓位跨度 5%–100%）测量：
`Q5 捕获率 = sum(系统在买入持有收益最高的 1/5 股票上的总收益) / sum(这些股票的买入持有总收益)`。

- pool_b_dev：`Q5捕获率 = 0.944 × 仓位 + 0.030`，**R² = 0.952**
- oos：`= 0.782 × 仓位 + 0.060`，**R² = 0.894**
- 逐格斜率 0.77–1.14，8 格中 7 格 R² 为 0.76–0.92（例外 oos|HK|挖掘期，斜率 0.40、R² 0.13）
- **没有任何系统在超过一个干净格里成为最大正残差**；在某格高于该线的系统在另一格必低于该线。

**操作性推论（强制条款）：任何"提高主升浪捕获率"的方案，必须报告其自身仓位所预测的捕获率，并战胜"游程置换零假设"**——在每只股票内部打乱该系统自身的持仓段与空仓段顺序，从而精确固定仓位、交易数与持仓时长分布。**迄今测试的方案无一做到。**

**这条定律统一解释了本项目的全部失败**：场景地图的恒等式分解（劣势 = 仓位缺口 -4.01（8/8），择时项 +0.66（0/8））、再入场家族全灭（Q5 捕获率在 0.386–0.436 间纹丝不动，基线 0.408）、以及四次风控放松尝试。**主升浪拿不到，不是因为出场规则差，而是因为仓位只有 41%。**

**主升浪检测器：有效但不定向（重要限定）。** 逻辑回归 + 61 个因果特征，仅用 pool_b_dev 2020-01..2023-10 拟合（222,997 根 bar，标签=此后 60 根内涨 ≥30%，正例率 16.43%），系数与阈值冻结后原样外推：**8/8 有效**——AUC 0.607–0.686，p90 阈值触发 7.7–14.1% 的 bar，命中率 24.5–40.2%（基准率 14.4–24.8%），提升度 1.51–2.26；p95 提升度 1.56–2.54，8/8。36 个标签变体（涨幅 20/30/50% × 视界 40/60/120 根）中 36/36 的 AUC 为 0.554–0.765。

**但它检测的是"即将有大波动"，不区分方向**；在 Q5 股票上的股内 AUC 仅 4/8（0.470–0.613）。这与全局图景一致：**风控门在波动放大时触发，检测器检测波动放大，而波动放大本身没有方向。** 因此它可作诊断，不能直接做择时。

**唯一改善两个规范案例的候选，仍未通过零假设：** `lit_kl_ma20_stop`（止损后武装 20 根、收复 MA20 回补、再入场腿用宽止损）把 603979 从 +19.1% 提到 **+63.1%**、002759 从 +1114.2% 提到 +1175.4%，两个干净池中位数 8.24→11.33 与 7.47→8.22，代价是仓位 +1.3pp、平均回撤改善 0.5–0.9pp。**但其 oos 中位数增益 +0.75pp 恰好落在随机再入场零假设自身区间（+0.75~+1.35pp）的下沿**——与"在随机时点加仓"不可区分。构建方明确不建议采用。

### 两个必须纠正的测量错误（2026-09-10，均由独立裁定方发现）

**错误一：用当日收益给"被门挡住的日子"归因是循环的。** 本项目此前报告"`trend_direction != 1` 挡住 82.9% 的空仓 bar、平均 -0.190%/bar、累计躲掉 -76,476pp，是策略价值的来源"。该测量用的是**被挡那根 bar 自身当天**的收益，而门本身由截至该 bar 的价格行为定义，因此结论是循环的。

**改用因果的下一根 bar 收益：被 `trend_direction != 1` 挡住的 413,112 根 bar，次日收益 +0.054 ~ +0.147%/bar，8/8 全为正**，而策略实际持仓的 bar 只有 +0.073 / +0.069%/bar。两个干净池合计**错过 +24,365pp**。**该门并不"物有所值"。** 规则：任何"某状态下该不该持仓"的归因，必须用**下一根 bar**的收益，不能用当根。

**错误二：事件集选错会凭空造出 alpha。** 文献方称"硬止损打在一个之后平均上涨的形态上，+2.60pp，8/8"。裁定方在**策略真正产生的硬止损离场 bar**（n=4,623）上重测：首阳线再入场 20 根收益 **+1.50pp**（8/8 为正）——但**同格无条件 20 根收益是 +1.43pp**，因此超额仅 **-0.04pp，只有 3/8 格为正**。

那个 +2.60pp 只存在于更宽的"5 日累计 ≤ -8% 形态"事件集（n=58,495），该集合确实有反转 alpha（超无条件 **+1.79pp，8/8**）——**但那不是策略的止损所做的事**。**规则：事件研究必须以策略实际触发的事件为集合，并以同格无条件收益为基准；缺少无条件基准的"8/8 为正"毫无意义。**

**配平时点对照证实：** 同样的武装事件、同样的触发频率、触发 bar 随机置换（`jg_klrand_s0/1/2`，仓位配平至 42.8–42.9% vs KL 的 42.68%）：pool_b_dev 上 KL 9.83 对随机 5.93/9.41/7.42；**oos 上 KL 9.92 对随机均值 9.93——完全打平**。KL 再入场赚的是市场无条件漂移，即**买了仓位**，不是择时。

### 止损打在一个之后平均上涨的形态上（已被上一节证伪，保留原始记录）（2026-09-10，文献 + 事件研究）

以"5 根累计跌 ≥8%"（正式硬止损的形状）为事件，44,111 个事件，两个干净池 × 两市场 × 两时期：

**`E[此后 20 根收益 | 5 根累计跌 ≥8%] = +2.17 到 +4.06pp，8/8 全正**（无条件基准 +0.46..+2.63pp）。即每次止损的"止损溢价"为 **-2.17 到 -4.06pp**，发生在 5.9–8.1% 的 bar 上。

同一批事件、同一 20 根视界下的再入场规则排名（全部 8/8）：

| 规则 | 20 根收益 | 捕获率 |
|---|---|---|
| 完全不出场 | +2.96pp | 100% |
| 止损后首根阳线即回补（Kaminski-Lo δ=0） | **+2.60pp** | **88%** |
| δ=+2% | +2.51pp | 85% |
| 点数图三格反转（离最低收盘 +6.1%） | +2.43pp | 82% |
| 收复 MA20 | +1.12pp | 38% |
| 海龟/Darvas 新 20 根高 | +0.37pp | 12% |
| **正式策略当前行为** | — | **最接近最后一档** |

**加宽止损已被排除，且有标定：** 正式的 8% 止损 = **2.08–2.27 个 N**（20 日 Wilder ATR），恰好是海龟的 2N；6% 止损 = 1.56–1.71 N。同一批事件上：不设止损 +2.92、10N +2.91、16% +2.50、12% +2.44、2N +2.44、8% +2.26——8% 只损失了 23% 的可回收边际。**"止损太窄"不是根因**，这与本项目 ATR 缩放止损六变体全部失败一致。

**追高门是锁死器，且锁死发生在特定窗口：** `dist_ma60 > 18` 在"≥30% 错过上涨"的空仓段里阻挡 **7.13%–11.43%** 的 bar，在无上涨的空仓段里只阻挡 **0.09%–0.46%**——**富集 17 至 121 倍，8/8 同向**。时间剖面：空仓第 0 根的阻挡者是 `trend_direction!=1`（60.2%）与 heikin 转空（21.9%），追高门仅占 0.5%；**到第 10–19 根追高门才接管**。**这解释了本项目四次"放松追高门"失败的原因——干预范围大了一个数量级，应只作用于出场后的再入场窗口。**

**西方主升浪模板在本市场为负向（重要否定）：** Weinstein/Minervini 趋势模板（close>MA150 & close>MA50 & MA50>MA150 & MA150 升 & 距 52 周高 25% 内 & 高于 52 周低 25%）fwd20 溢价 **7/8 为负**，fwd40 **7/8 为负**；O'Neil 八周持有触发在 A 股 **4/4 为负**；海龟 55 根新高 4/8；Mansfield 相对强度 6/8 为负、A 股 4/4 为负。资本利得悬置（CGO）高档 **8/8 跑输**、低档 8/8 跑赢——与 Grinblatt-Han 的符号完全相反。

**必须同时记录的最大风险（文献方自陈，本项目认同）：** 该规则的核心主张是"急跌之后会回来"，而 pool_b 与 oos230 **均不含退市股**——这正是无退市股的池子最会美化的一类主张。外部文献对该偏差的量级估计：道指 30/标普 100 趋势跟踪约 3.7pp CAGR；横截面动量在加入退市股后由 16.4% CAGR 变为 83% 最大回撤。**现有数据无法量化本项目所受的偏差，因此任何正向结论都必须附带此保留。**

### 卖飞的根因：风控触发器与主升浪是同一个信号（2026-09-10，五路独立场景分析）

**不是参数太紧，是策略的身份。** 策略的每一道风控——ATR 扩张门（ATR > 20 日均值 ×1.5 禁入）、追高门（偏离 MA60 > 18% 禁入）、固定百分比硬止损（6%/8%）、5–6% 浮盈即启动的移动止盈——**触发条件都是"波动放大 / 偏离均线"，而主升浪的定义恰恰就是波动放大加偏离均线**。风控越有效，越必然错过主升浪。

五个互不相关的场景族给出同一结论，全部 8/8 格：

| 证据 | 数字 |
|---|---|
| 自身波动前 1/3 的日子，买入持有 | **+11.55 bp/日，8/8**（滞后 2/3/5/10 根仍 8/8；25 次逐季剔除全部 8/8） |
| 同一批日子，趋势策略减买入持有 | **-7.16 bp/日，8/8**，仓位 43.7% vs 100% |
| 高波动 × 跌破全部均线 | 买入持有 **+22.92 bp/日，8/8**，七年全正 |
| 量价相关性低的日子 | 趋势策略仓位降到全族最低 **19.9%**，而买入持有赚 +7.28 bp |
| 高波动 × 低换手（双重排序） | 趋势策略 **-15.20 bp/日** |
| 超额的方差分解 | **静态仓位项解释 85% 的横截面方差**（趋势 0.85、高抛低吸 0.82、hem 0.59） |
| 每个持仓日的收益 vs 买入持有每日历日 | 趋势策略 **8/8 格为负**，均值 -4.60 bp——连持仓那天也跑不赢单纯持有 |
| Spearman(窗口买入持有收益, 策略超额) | **8/8 全负**：趋势 -0.643、高抛低吸 -0.867——股票涨得越多，策略相对越差 |

**推论一：** 策略的超额几乎全部来自"仓位低"这个静态事实，而非择时能力。因此在上涨的股票上它必然跑输，在下跌的股票上必然跑赢——这与"空仓期收益决定一切"（相关 -0.94~-0.96）是同一件事的两种测法。

**推论二：** 单纯拆掉某道门无效，已实测。拆追高门吃到右尾（均值 +15%）但同时吃到所有下跌（回撤、盈亏比、Sharpe 全面变差）。**因为门只看幅度、不看方向。**

**逐笔归因（`/tmp/.../rootcause.py`，三个池 22,854 笔交易）。** 定义：一笔交易出场后 60 根内股票继续涨 ≥20% 即为卖飞。**卖飞率在三池高度一致：30.8% / 30.5% / 30.2%。**

| 出场机制 | 笔数 | 占比 | 卖飞率 | 平均收益 | 平均峰值 | 回吐 | 出场后 60 根涨幅 |
|---|---|---|---|---|---|---|---|
| 硬止损 6/8% | 7051 | 30.9% | **35.8%** | -9.2% | +1.8% | +11.0% | +21.0% |
| 趋势转空 | 9593 | 42.0% | 26.1% | +10.0% | **+24.7%** | **+14.7%** | +16.8% |
| 移动止损 | 3191 | 14.0% | 33.7% | -2.5% | +11.1% | **+13.6%** | +19.4% |
| 移动止盈 | 2204 | 9.6% | 32.4% | +0.9% | +10.2% | +9.3% | +19.6% |

损失总量：硬止损 2,524 笔卖飞、错过 **115,172pp**；趋势转空 2,502 笔、**111,225pp**。两者各占一半。移动止损平均峰值 +10.73% 却以 **-1.12%** 收场，**回吐 11.85%——比平均一笔赚的还多**；趋势转空只捕获峰值的 40%（+10.0 / +24.7）。

**固定百分比止损是根因的可量化载体。** 硬止损距离以股票自身日 ATR 衡量，中位数仅 **2.20 个 ATR**，且随波动率**反向缩放**：

| 入场时波动 | 日 ATR | 止损距离（ATR） | 卖飞率 |
|---|---|---|---|
| 最平静 | 2.73% | 3.12 | 30.5% |
| 较平静 | 3.65% | 2.43 | 35.4% |
| 波动 | 4.47% | 1.99 | 36.8% |
| 最波动 | 6.09% | **1.55** | **39.4%** |

四档完全单调。**1.55 个日 ATR 是噪声级别**——上涨中的股票日常回调即可触发。即：**波动越大的股票止损越紧，而波动大的股票正是会起飞的那些。** 注意该波动–卖飞关系为 **6/8 格为正**（A 股验证期两池为负，-1.2pp 与 -7.6pp），方向成立但未达本项目 8/8 的确立标准。

**由根因直接导出的修复已实测，失败。** 在 `research/hooked.py` 增加 `STOP_ATR_K`（硬止损 = K × 入场时股票日 ATR%，clip 到 3–25%，默认关闭故自检仍 305/305 通过），pool_b_dev 292 只：

| | 中位数 | 均值 | 回撤 | 胜率 | 最差单笔 |
|---|---|---|---|---|---|
| 基线（固定 6/8%） | **8.24** | 49.80 | -51.94 | 35.13 | -36.12 |
| ATR × 3 | 5.54 | 48.98 | -52.89 | 36.56 | -29.56 |
| ATR × 4 | **4.18** | 49.07 | -53.07 | 36.74 | -28.84 |

胜率与最差单笔改善，但**中位数腰斩**——放宽止损同时让持仓在亏损股上停留更久。

**方向条件化也失败（第四次撞击）。** 在 `hooked.py` 增加 `STOP_WIDE_MODE`（仅在确认上升趋势内放宽止损）并做入场侧对应改动（仅在同样条件下放松追高门），pool_b_dev 292 只：

| | 中位数 | 均值 | 回撤 | 盈亏比 | Sharpe | 前五集中度 |
|---|---|---|---|---|---|---|
| 基线 | **8.24** | 49.80 | **-51.94** | **1.43** | **0.78** | **21.81** |
| 方向止损 ma120 k6 | 6.59 | 48.50 | -52.82 | 1.41 | 0.76 | 21.17 |
| 方向追高门 ma120 | 6.44 | 51.76 | -53.43 | 1.40 | 0.75 | 26.20 |
| 方向追高门 近高点 | **9.01** | 52.30 | -53.29 | 1.40 | 0.76 | 25.97 |
| 两侧同时放宽 | 5.75 | 49.98 | -54.38 | 1.39 | 0.72 | 24.94 |

只有"近高点"变体的非配对中位数更好（+9.3%），但风险指标全面变差、集中度 21.81→25.97。**逐股配对检验后 0/8：八格差值中位数全部恰为 `+0.00`，仅 7.6%–21.7% 的股票受影响。** 又是少数股票造成的分布平移——"非配对中位数陷阱"第二次命中，该条规则已证明是必需的。

**为什么四次都失败，场景地图给出了答案：** 策略的劣势是**仓位缺口**（-4.01，8/8），而择时/选股项是 **+0.66 且 0/8**——**没有可供放大的选股能力**。因此任何增加仓位的方式（拆门、放宽止损、按方向条件化）都只是买回市场 beta，同时按比例买回 beta 的风险。这不是参数问题，也不是条件化方式的问题。

**这是同一堵墙的第三次撞击**（追高门、成交量弱门、ATR 止损）：**任何按"幅度"均匀放松风控的改动，都是拿右尾换风险，净效应为负或零。** 根因诊断成立（止损在波动股上仅 1.55 个 ATR，是噪声级别），但按幅度放宽不是解。**唯一还未被否证的方向是按"方向"条件化**——同为高波动/大偏离，价格在上升均线上方与下方必须区别对待。

### 改动必须验证"确实生效"，不能只看脚本打印成功（2026-09-10 的坑）

给 `hooked.py` 加"方向条件化止损"时连犯两个错误，五个变体跑出与无条件版本**逐位相同**的结果才被发现：

1. 用脚本做字符串替换时，锚点里的 `%` 与文件中的 `%%` 不匹配，**替换静默未生效**，而脚本仍打印了成功信息。
2. 即使替换成功也没用：条件列 `_wide_ok` 是在 `analyze()` **返回之后**才写入的，而仓位状态机在 `analyze()` **内部**就已执行完毕，根本读不到该列。

**该规则当日第三次命中（Kaminski-Lo 再入场）：** 在 `_build_position_series` 里新增的再入场判断中误用了局部变量 `price`，而 `price = close_arr[i]` 的赋值位于该判断**之后**，因此块内 `price` 仍是上一根 bar 的收盘价。后果：`up` 模式的判据 `price / prev_close[i] - 1 > 0` 恒等于 `0.0 > 0.0`，**永不触发**（四个变体与基线逐位相同）；`ma20` 模式则用上一根收盘价与当根 MA20 比较，会触发但比较的是错误的量，**该组结果同样作废**。改用 `close_arr[i]` 后三个变体的交易数才出现差异（26 / 30 / 28）。

**规则：** 任何对 `hooked.py` / 候选类的改动，运行回测前必须做两步验证——(a) `grep` 确认代码真的落盘；(b) 打印各候选类的关键属性，确认它们**互不相同**。若两个本应不同的候选给出逐位相同的指标，一律先当作接线失败，不要解释成"这两个方案效果一样"。状态相关的列必须在状态机能读到的时点计算（本例改为直接在 `_build_position_series` 内部由 `data["close"]` 现算）。

### 卖飞与踏空必须分开：前者常见但与盈亏关系弱（2026-09-10 逐股分析）

按"出场后 60 根内继续涨 ≥20%"定义的卖飞，逐股统计（pool_b_dev + oos，522 只）：

- 卖飞率 **30% 且高度均匀**——前 10% 的股票只占错过总量的 21–22%，后一半占 27–28%。这是结构性现象，不是少数特例。
- **但它与实际超额的相关只有 -0.19 ~ -0.22**，远弱于空仓期实现收益的 **-0.94 ~ -0.96**。原因：卖飞用的是出场后的**峰值**，而许多"错过的涨幅"在策略重新入场前已经跌回。**决定盈亏的是空仓期实际实现的收益，不是峰值。**

具体案例说明两者必须分开：

| 股票 | 买入持有 | 策略 | 超额 | 卖飞错过 | 真正的问题 |
|---|---|---|---|---|---|
| 603538 | +120% | +342% | **+222pp** | 1098pp | 卖飞最重却是大赢家 |
| 002885 | +130% | +388% | **+258pp** | 893pp | 同上 |
| 03939 | **+4473%** | +279% | **-4194pp** | 仅 758pp | **踏空**，不是卖飞 |

**规则：报告本类问题时必须区分"卖飞"（出场后继续涨）与"踏空"（根本没进场）。对最大的赢家，主要损失来自后者——40% 的仓位在一只涨 45 倍的股票上，无论出场规则多好都拿不到。** 同为高波动，价格在上升均线上方是主升浪应持有，在下方是危险应规避；现有的门对两者一视同仁。任何修复方案都必须在这个轴上做文章，并接受 8 格检验与匹配仓位零假设。

### 入场门消融的实测，以及"非配对中位数"陷阱（2026-09-10）

空仓期归因（见上节）显示四个"安全门"只挡住 6.5% 的空仓 bar 却错过约 +5.6 万 pp。**实测把这个朴素归因推翻了**：错过最多的门不是最该拆的门。pool_b_dev 292 只，逐个拆门：

| | 均值 | 中位数 | 回撤 | 盈亏比 | Sharpe |
|---|---|---|---|---|---|
| 基线 | 49.80 | 8.24 | -51.94 | 1.43 | 0.78 |
| 去追高门（归因排第 1，+19814pp） | **57.12** | 8.74 | **-53.35** | 1.41 | 0.76 |
| 去 M 头 | 50.91 | **7.15** | -52.77 | 1.42 | 0.78 |
| 去 ATR 扩张 | 48.38 | **6.97** | -52.33 | 1.41 | 0.77 |
| 去成交量弱门（归因排第 4，+7941pp） | 51.31 | 9.52 | -52.26 | 1.43 | 0.79 |
| 四门全去 | 51.42 | 8.21 | **-54.77** | 1.39 | 0.75 |

拆掉追高门是**用风险换右尾**（均值 +15%，但回撤、盈亏比、Sharpe 全面变差，前五赢家集中度 21.81→28.48）。**教训：空仓期的朴素归因只能提出假设，不能得出结论——拆掉一个门会改变整条路径，路径变化比错过的 bar 更重要。**

**"非配对中位数"陷阱（本项目的新坑）：** 成交量弱门看似最干净（中位数 8.24→9.52，+15.5%，Sharpe 与盈亏比不降），但逐股配对检验后 **0/8 格为正**：7 个格子的配对差值中位数恰为 `+0.00`，1 个为 `-0.11`，改善的股票只占 25.5%–42.5%，Q5 捕获率几乎不动（0.450→0.468）。原因是该门只在少数股票上真正起作用，绝大多数股票结果完全相同，分布的轻微平移抬高了跨股票中位数。

**必须同时报告"仅受影响股票"的配对统计量（2026-09-10 用户指出后修正）。** 一道只对部分股票起作用的改动，天然让未受影响的股票差值恒为 0，因此"全体配对中位数"在受影响比例 < 50% 时**几乎必然给出 +0.00**——它看不见效应，不是效应不存在。以 `ma60_12` 为例（受影响 48.9%–68.5%）：

| 统计量 | 8 格结果 |
|---|---|
| 全体股票配对中位数 | **1/8** |
| **仅受影响股票的配对中位数** | **8/8**（+0.39 ~ +5.96） |
| **受影响股票中改善的占比** | **8/8 全部 >50%**（53.5%–76.6%） |
| 仅受影响股票的配对**均值** | **4/8**（含 -38.46、-25.82） |

**正确做法：同时报告 (a) 受影响股票占比、(b) 受影响股票的配对中位数与改善占比、(c) 受影响股票的配对均值。** 中位数 8/8 而均值 4/8 是"**赢得频繁、输得巨大**"的签名，必须原样呈报，不能只取其一。此前用 (a) 缺失的口径否决 `ma60_12`、`ac_*`、`dirchase_nearhigh`、`ug_vol` 四个候选，其中 `ma60_12` 的否决理由需按本节重估。

**规则：`harness.py` 报告的中位数是跨股票的非配对统计量，任何改动都必须再做逐股配对检验。但配对检验必须按上表的三个统计量报告，不得使用"全体股票配对中位数"作为否决依据——该统计量在受影响比例 < 100% 时功效极低甚至为零。**

**代数事实（2026-09-11 独立裁决确认）：** "全体股票配对中位数 > 0" 要求**超过一半的全部股票**严格改善。本轮受影响比例 49%–69%，反推对受影响股票的胜率要求是 **0.73–1.02**；其中 pool_b_dev 港股验证期未受影响比例 51.1%，要求 **>1.00——任何改动都数学上不可能通过**。教科书配对检验（符号检验、Wilcoxon）按构造丢弃平局，正是为此。**这不是"标准严"，是"零功效"。**

### 不许停在整体均值上（2026-09-10，用户明确要求）

用户原话：“不是看整体就完事了，即便整体一般，你也要看出来有没有好的？有没有差的？原因是什么。”“不是说所有的股票都一刀切，你要做出权衡，什么场景下什么策略效果好。”

**规则：** 任何策略评估的产出，除整体指标外**必须**包含三层：

1. **分布**：按场景状态（波动、趋势、震荡、量价）与按股票-时段两个维度切开，报告每个子集里各策略的收益、每单位仓位的收益、仓位，以及谁最好。
2. **好与差**：明确指出哪些子集收益高、哪些差，各占多大比例、贡献多少；“整体一般”从来不是结论，它只是各子集相互抵消的表象。
3. **原因**：对好与差各给出可检验的机制假设，并在两市场 × 两时期 × 两干净池的 8 个格子里检验；检验不过的写“未知”，不许编叙事。

同时记录本节诞生时踩的坑：**按状态分桶做归因时，状态必须用前一根 bar 收盘时已知的值**。第一版用第 t 天的 20 日波动率给第 t 天的收益分桶，得到“低波动状态下策略跑赢买入持有 8/8”；因为第 t 天的收益本身在那 20 日窗口里，滞后一根 bar 后该结论变成 2/8——完全是归因层的未来信息。这是本项目第三次因分组方法得到反直觉结论（前两次：用被信号污染的状态变量分组；在 30 只样本上分组）。

滞后一根后真正稳定的：**自身高波动状态（前 1/3）之后，买入持有每日 +5 到 +18.5 bp、为低波动状态的 2–10 倍，8/8 同号且滞后 5 根仍成立；趋势与高抛低吸此时跑输买入持有 8/8，因为它们恰在此时仓位最低。** 波动率状态仅在一个月尺度有持续性（20 根后同桶 42–45%，60 根后 34–37%，120 根后随机），因此“某股是高波动股”不是长期属性。信号质量（每单位仓位收益）不随波动状态稳定变化——“按波动率动态调参”没有靶子。

### 池内证据的上限（2026-09-09 的决定性案例）

一个候选（IBS 回调策略的“质量门”，取 ADX(7) 自身分位与相对成交量自身分位的均值，低于阈值不入场；同一门叠加到主策略上称 `main_q50`）在开发池上通过了迄今为止最完整的证据链：

- ALL305 五项主指标四项改善一项持平：平均回撤 `-48.89% -> -47.71%`、收益中位数 `43.77% -> 47.64%`、平均胜率 `37.67% -> 38.39%`、同公式盈亏比 `1.94 -> 2.04`、盈利股票占比持平；组合 Sharpe `1.15 -> 1.21`，前五赢家集中度 `26.91 -> 17.70`。
- **七个分组的盈亏比与 Sharpe 无一例外上升**。
- 独立对抗方从文字规格重实现，数字逐项复现。
- 相对“匹配暴露与持续性”的随机门（80 个种子）高出 `9.3` 个标准差；**镜像规则（只取低分一半）在相同交易笔数下低于零假设 `7.4` 个标准差**，两侧对称。
- 月度分块自举（2000 次，处理时间聚集）：均值提升 `+0.159`，置信区间 `[0.074, 0.240]`，`p < 0.0001`。
- 仓库自带的正式未来函数检测 `7/7` 通过、`0` 失败；前缀回放 `840` 点 `0` 差异。
- 当时认为机制清晰：ADX 分位与成交量分位衡量趋势的真实参与度。**该机制解释事后被证伪**——趋势陡峭程度对 IBS 每笔收益并无稳定预测力（全池与干净池方向相反，四格符号不一致），所以“机制可解释”当时是一种事后叙事，不是独立证据。

**它在池外 230 只上失败。** 收益中位数 `7.47% -> 6.04%`、盈利股票占比 `56.52% -> 53.91%`、亏损股 `100 -> 106`、组合 CAGR `6.07% -> 5.36%`、Sharpe `0.70 -> 0.66`。分市场看：A 股完全反向（中位数 `3.00% -> -1.91%`、盈亏比 `1.30 -> 1.24`），港股基本持平。

**由此确立的规则：**

- 上述任何一项池内证据，以及它们的任意组合，都**不足以**预测池外表现。多重零假设、镜像对称、分块自举、前视检验、分组一致性、机制可解释——全部通过仍可能在新股票池上翻负。它们只能用于**排除**错误，不能用于**确认**有效。
- 因此，任何策略改动进入正式源码前，**必须**先通过池外验收；池内证据无论多完整都不构成充分条件。
- 一天之内已有三个候选倒在池外验收：追高门 `18% -> 12%`、横截面收益排序、本条的质量门。三者在开发池上都表现优异。这不是偶然，是开发池自身携带事后选择（305 只中 150 只带价格路径标签）的必然结果。
- 记录否定结论时必须同时记录它**通过了哪些检验**，否则后人会重复实现同一个候选并再次被池内证据说服。

### 追高闸门"按股票自适应"——被否，且方向与直觉相反（2026-09-10，用户提议）

固定 18% 阈值在不同股票上含义差异极大：日 ATR 2.86% 的平静股相当于 6.3 个 ATR（触发率仅 **2.7%** 的 bar），日 ATR 4.54% 的活跃股只有 4.0 个 ATR（触发率 **11.4%**）——**同一阈值在活跃股上的触发频率是平静股的 4.2 倍**。据此测试三族自适应闸门（全部因果）：按自身 ATR 缩放（K=3/4/5/6）、按自身 250 根分位（q=0.85/0.90/0.95）、按自身 `dist_ma60` 滚动标准差（K=2/3）。

**九个变体在两个干净池上全部劣于统一收紧的 `ma60_12`**（pool_b_dev 中位数 ma60_12 **10.41**，九变体 4.26–8.52；oos ma60_12 **10.08**，九变体 1.70–8.94）。

**机制**：ATR×4 给最活跃组的阈值是 **18.2%**，与固定 18% 几乎相同——它只收紧了平静股。而 ma60_12 收紧了**全部**股票。**因此增益来自收紧活跃股，而非放宽它们：这道闸门上"给波动大的股票更多空间"的直觉是反的**（与固定百分比止损的情形相反——止损那里波动缩放同样失败，但原因是持有亏损股更久）。

### 三方案完整比较（2026-09-10，`research/three_options.py`）

同一批股票、同一批交易日、同一费率（A 股买 1.5bp/卖 6.5bp，港股双边 14.5bp，按 |Δ仓位| 计费），两个干净池结果同形。

**A 混合（w 份买入持有 + (1-w) 份策略，逐股）** — pool_b_dev：

| w | 资金占用 | CAGR | 组合回撤 | Sharpe | Calmar | 每股中位 | 跑赢 B&H |
|---|---|---|---|---|---|---|---|
| 0.0 | 40.6% | 6.51% | **-8.8%** | 0.78 | **0.74** | 8.25% | 39.0% |
| 0.2 | 52.5% | 8.18% | -12.7% | **0.81** | 0.64 | 17.82% | 45.2% |
| 0.4 | 64.4% | 9.79% | -16.4% | **0.81** | 0.60 | 26.30% | 47.3% |
| 0.6 | 76.2% | 11.35% | -20.1% | 0.80 | 0.56 | 34.81% | 50.3% |
| 0.8 | 88.1% | 12.85% | -23.6% | 0.79 | 0.54 | **39.37%** | **54.8%** |
| 1.0 | 100% | **14.28%** | -27.1% | 0.78 | 0.53 | 32.76% | — |

oos 同形（Sharpe 峰在 w=0.4 为 0.76，每股中位峰在 w=0.8 为 33.11%）。**最优 w 依目标而定，无一 w 全面占优。**

**B 只当风控叠加层** — 组合 CAGR 14.28→6.51（**-7.77pp**）、波动 19.50→8.49、**最大回撤 -27.05→-8.84（改善 18.21pp，降 67%）**、**Sharpe +0.00**、Sortino -0.01、Calmar 0.53→0.74。每股中位回撤 -63.4→-51.9，**77%（oos 82%）的股票更浅**。**它买到的是回撤，不是风险调整后收益。**

**C 跨股票调配闲置资金（信号不变，只提高资金利用率）——失败且信息量大。** 利用率 40.6%→99.9% 后：pool_b_dev CAGR 12.59% 对买入持有 14.28%、Sharpe 0.71 对 0.78；**oos CAGR 10.81% 对 13.67%，回撤 -35.1% 反而差于买入持有的 -28.1%**，Sharpe 0.63 对 0.74。每日有信号股票中位 118/292（40%）。

**结论：把资金集中到策略每天选中的那 40% 股票上，不如平摊到全部——策略在组合层面的选股是负贡献。这彻底否证了"策略有选股价值、只是资金效率低"的假设**，与"择时项 +0.66 且 0/8"、"游程置换分位 22/28"互相印证。

**最终形态：A 与 B 是同一条曲线的两端，真正的决策只有一个数字——用多少 CAGR 换回撤。价格近似线性：全程放弃约 7.8pp CAGR 换 18.2pp 组合回撤改善，而 Sharpe 全程几乎不变（0.78→0.81→0.78）。这是风险偏好选择，不是效率改进。**

### 为什么主升浪模板在本市场为负：它是滞后确认，点火点成簇落在顶部（2026-09-11）

Weinstein/Minervini 阶段二模板（收盘 > MA50、> MA150，MA50 > MA150，MA150 上升，距 52 周高点 25% 以内，高于 52 周低点 25%），逐日叠加到 10 只代表股（覆盖全部五种失败模式）上，工具 `research/advance_charts.py`，图在 `research/out/advance_charts/`。

| 股票 | 买入持有 | 模板点火后 60 根 | 同股无条件 | **超额** | 状态内 | 状态外 |
|---|---|---|---|---|---|---|
| 300502 | +4965% | -3.8% | +11.1% | **-14.9%** | +13.3% | +10.1% |
| 300394 | +3020% | +0.9% | +9.5% | **-8.6%** | +4.0% | +11.4% |
| 300757 | +2327% | -4.9% | +9.0% | **-13.9%** | -6.5% | +14.9% |
| 300751 | +522% | -17.0% | +3.1% | **-20.1%** | +2.9% | +3.1% |
| 03896 | +172% | -31.7% | -1.8% | **-29.9%** | -16.0% | -1.3% |

10 只**中位超额 -8.16pp**。

**关键区分（此前未分离出来）：处于状态内的前向收益（中位 +1.6%）与状态外（+1.8%）几乎相同——"待在主升浪状态里"本身无害；有害的是"刚刚进入"那一刻。**

**机制（图上可见）：** 六个条件必须同时成立，只可能发生在一段持续上涨**之后**；而状态会反复开关，于是**在顶部区域反复点火成簇**。300751 为例：2020 年低位起步时点火 +75%/+58%，2021 年顶部一簇 **-30%/-24%/-19%/-20%/-18%**，2022 年第二个顶 +2%/-12%/-7%/-8%/-17%，2026 年起步 +63%/+39%/+38% 而冲顶后 -15%/-45%。**同一只股票内，一轮周期的首次点火赚钱，随后的重复点火亏钱。**

**"首次点火赚钱"假设被否（我又一次从单张图上读出不存在的规律）。** 12,718 次点火按"状态此前沉寂时长"分档：顶部重复点火（≤20 根）超额 -1.5%，长期沉寂后首次点火（>250 根）**-3.5%、胜率 40.3%——反而更差**，8 格仅 4/8。300751 那张图不具代表性。

**真正 7/8 成立的是"已涨幅度"：** 点火时该股前 120 根已涨 <0% 超额 -1.3%、0–30% -1.0%、30–80% -2.9%、**>80% -7.3%（胜率 36.9%）**。**但每一档都是负的**——确认动作在任何阶段都是负期望，惩罚随已涨幅度放大。

**由此导出的改进方向（已测）：把主升浪状态用作"持有条件"而非"入场条件"。** 入场路径不动，状态成立时压制常规出场，状态破坏时离场。结果：

| 候选 | 均值 | 中位数 | 平均回撤 | 盈亏比 | Sharpe | 受影响配对 | 等仓位对照 |
|---|---|---|---|---|---|---|---|
| 基线 | 49.80 | 8.24 | -51.94 | 1.43 | 0.78 | — | — |
| `hold_s2`（阶段二） | 52.58 | **12.65** | -51.51 | 1.46 | 0.79 | 4/8 | **3/8**（P≈54%）✗ |
| `hold_stack`（均线多头排列） | **55.51** | 6.99 | **-49.88** | 1.43 | **0.84** | **7/8** | **5/8**（P=44.5%） |

`hold_stack` 全池指标几乎全面改善（P25 21.79→25.40、P75 68.96→76.47、组合回撤 -8.85→-8.49、胜率 35.13→36.01），**且是在更低仓位下（42→37%）**；受影响股票配对中位数 7/8 为正、改善占比 7/8 超 50%（最高 69.7%）。**但对双向等仓位随机对照只有 5/8，按项目标准属未确立，不合入。**

**拆解与 oos 复核后被否（2026-09-11 收尾）。** `hold_stack` 由两个动作组成：(a) 排列完好时压制常规出场、(b) 排列破坏时新增出场。逐一测量：

| pool_b_dev | 均值 | 中位数 | 平均回撤 | Sharpe |
|---|---|---|---|---|
| 基线 | 49.80 | 8.24 | -51.94 | 0.78 |
| 仅 (a) 压制出场 | 50.45 | **8.24** | -51.99 | 0.78 |
| 仅 (b) 破位出场 | **55.36** | 6.99 | **-49.79** | **0.84** |

**(a) 完全无效（中位数逐位相同），全部效果来自 (b)。因此该候选不是"主升浪满仓"，而是"结构破坏时更快离场"——这也解释了它为何降低仓位，且说明它并未实现"主升浪满仓"这一目标形式。**

**oos 复核推翻它：** 均值 43.91→**42.46**（更差）、盈亏比 1.39→**1.36**（更差）、组合 CAGR 6.07→**5.67**（更差）、**Sharpe 0.70→0.70（完全不复现）**。pool_b_dev 上那个 0.78→0.84 是池内特有。两池唯一一致的仍是平均个股回撤改善约 1.5–2pp。**结论：该线关闭。**

**与 `ma60_12` 的区别值得记录**：ma60_12 对等仓位对照是 **2/8（比随机减仓更差）**，hold_stack 是 **5/8（比随机略好但不显著）**。两者都不过关，方向不同。

**测试自身的坑（当日新增）：等仓位对照必须双向。** 首版对照只实现了"延长持仓段"（假设候选加仓位），而 `hold_stack` 实际**降低**仓位，于是对照对多数股票返回 NaN，只在少数仓位恰好上升的股票上算出结果，给出虚假的 7/8。**规则：写等仓位对照前先测量候选的仓位变化方向，并在代码中同时实现 trim 与 extend 两支；若某格的对照样本数远小于该格股票数，一律视为对照失效。**

**因此"主升浪策略为负"不是模板选错指标，而是"确认"这个动作本身在本市场要付代价**——与已确立的 A 股横截面动量 IC 为负、CGO 符号反转（高档 8/8 跑输，与 Grinblatt-Han 相反）、O'Neil 八周持有 A 股 4/4 为负是同一件事。

### 标准结论修正：出现了两条通过全部可做检验的"加法式叠加"（2026-09-11）

此前的标准结论是"策略是降回撤叠加、无择时能力、无物可并入"。**前两句仍然成立，第三句不再成立。**

两条候选都**不改动正式策略任何一行**，只在正式策略**空仓**时补仓——正好命中恒等式（超额 = 空仓期收益的相反数）所指的位置。

**候选 A：预判叠加 `anti_ma120m2_q90_h20`。** 检测器触发（`research/launch_model.json["full"]` 的**冻结** q90 阈值 0.26824，绝非在评估数据上重算的分位）**且** `(close-MA120)/ATR14 <= -2.0`（非动量超卖）→ 正式策略空仓时以收盘价买入，持有 20 根，12% 硬止损；到期时若正式策略已转多则**交接**，否则平仓。

| | dev 基线 → 候选 | oos 基线 → 候选 |
|---|---|---|
| 中位数 | 8.24 → **27.52** | 7.47 → **28.76** |
| Sharpe | 0.78 → **1.05** | 0.70 → **0.96** |
| CAGR | 6.52 → **10.48** | 6.07 → **9.98** |
| 仓位 | 40.9 → 46.1% | 41.2 → 47.0% |
| 20bp/边滑点中位数 | **-2.96 → +14.16** | **-3.50 → +13.78** |

受影响 83.9%/84.8%；配对中位数 **+25.79/+21.52**，符号检验 **83.3%（p=3.4e-27）/85.1%（p=1.6e-24）**，**配对均值与中位数同号**（+46.65/+34.79）；**8/8 格为正**；六档参数平台；日历轮转与**双向**同仓位随机择时零假设两池 **8/8 全胜且对照落回基线**；前缀回放 **1619/1619 零差异**。**比它依附的系统更耐成本。**

**机制**：26.9% 的腿在到期时把活多头交给趋势系统，这些腿均值 **+22.60%**、97.9% 为正、**贡献腿总收益的 104%**；其余 73% 的腿均值 -0.29%。**全部经济内容是抢在趋势系统入场前最多 20 根进场。**

**图表取证发现了第三条减分，聚合统计看不见它（2026-09-11，`research/overlay_charts.py`）。** 逐腿画出 10 只代表股：184 条腿中**交接腿均值 +16.4%，非交接腿均值 -2.9%**，全部价值在那 33% 的交接腿上。而失败案例有统一签名——**腿数多、交接率低**：

| 股票 | 买入持有 | 正式策略 | **加叠加** | 腿数 | 交接率 |
|---|---|---|---|---|---|
| 300274 | +1020% | +4250% | **+7666%** | 13 | 23% |
| 300751 | +522% | +2646% | **+6372%** | 16 | 50% |
| 03896 | +172% | -34% | **-63%** | 23 | 26% |
| **00909** | **-95%** | **+152%** | **-58%** | **48** | **15%** |

**00909 是决定性案例**：股票跌 95%，正式策略靠空仓赚 +152%，叠加一路买跌 48 次把它变成 -58%。图上可见机制——**下跌趋势中 `(close-MA120)/ATR` 连年低于 -2，超卖条件永久为真；而崩塌股波动率高，检测器也几乎连续触发**。两个条件长期同时成立。**这正是无退市股的池子所掩盖的形态。**

**三种护栏全部实测失败，得不偿失：**
- **按 MA120 斜率过滤**：崩塌档（<-15%）腿均值 **+3.66%**，上升档（>0%）**+3.69%——均值几乎相同**，只有中位（+0.53 vs +4.16）与胜率（51.1% vs 58.1%）不同，说明暴力反弹补偿了亏损；8 格 **3/8**。
- **要求站上 MA250**：完全反向——MA250 下方腿均值 **+4.22%**、胜率 59.4%，上方仅 **+0.32%**、49.7%。该规则本质是买跌。
- **熔断器**（滚动 250 根内最多 N 条腿 / 连亏 K 条停手）：**约束越紧越差且两池单调**——中位数 dev 27.52→28.11(L6)→26.95(L4)→22.24(L3)→21.14(L2)，oos 28.76→28.48→26.91→24.24→22.79；连亏熔断更差。522 只中腿数 >35 的只有 8 只，任何有意义的上限都会砍掉远多于这 8 只的盈利腿。

**因此：尾部风险真实、机制清楚、且目前无法在不损伤整体的前提下消除；而池内无退市股意味着真实尾部比测得更差。这必须作为该候选的已知缺陷随其一同记录。**

**两条减分（必须原样引用）**：① **检测器本身不带来每股收益**——对纯 dip 的配对比较 dev **-0.18（符号 146/146，p=1.00）**、oos +0.77（p=0.95）；检测器买到的只是**仓位效率**（46% 对 69% 仓位换同等收益，MDD -11.25 对 -21.41）。② 严格同仓位对照下 **oos 个股符号检验 p=0.208 不显著**（组合层两池仍胜）。

**候选 B：换月日历叠加，仅 A 股、仅交易所日历实现形式。** 100% 受影响，配对中位 +23.14/+23.86，8/8 格，同仓位随机对照全部落回基线以下（随机多持一天是亏钱的）。**三条硬伤**：① **无参数平台**——偏移 -1 有效，偏移 **+1 显著为负**（Sharpe 0.672），-2/-3 更差，是单点；② 轮转 p 下限 6.2e-4（标签向量长 1619，只有 1619 种轮转），**高于其自身 Bonferroni 阈 4.1e-4**，"唯一通过多重检验"不成立；③ 外部指数复核：2000–2019 月首日 +22.1bp（t=1.85），**2013–2019 仅 +6.1bp（t<1.2）**，而 2020–2026 为 +40.6bp——**回测窗口落在效应最肥的一半，真实前瞻幅度约为回测的三分之一**。原实现 `cal_tom_k1` 有**未来函数**（读下一根日期，208 处差异），只有 `rf_tomcal_k1` 干净。港股版 20bp 下劣于基线。

**同批死亡的两条**：`fe_none` 的头条是**未配对的"中位数的中位数"**，配对后中位 **-0.41/-0.39**、符号 39.1%/41.7%；波动率管理仓位八格 CAGR 全低于基线，且其"最优"形式实际是在平静期加杠杆，与"不确定时减仓"方向相反。

**未经攻击的新组合**：预判叠加 × 换月叠加 = dev 中位 52.80 / Sharpe 1.24 / CAGR 13.73 / MDD -11.00 / 仓位 48.8%，oos 53.51 / 1.14 / 13.09。**未经第三方攻击，按未验证对待。**

### pool_c 验收结果：预判叠加通过（2026-09-11，池已花掉）

**一次性单批，全部对照同批运行，无重跑无调参。** `resolve_pool()` 已加入 `"pool_c"`。

**Performance (pool_c CN-A 280，预注册判据)**

| | 基线 | `anti_ma120m2_q90_h20` |
|---|---|---|
| Return | 31.51% | **72.80%**（+131%） |
| Median | **-1.79%** | **+26.07%**（由负转正） |
| tPF | 1.31 | **1.52**（+16.0%） |
| Win rate | 34.49% | **38.26%**（+3.77pp） |
| Max drawdown | -53.71% | **-53.11%**（改善 0.60pp） |
| 组合 CAGR / MDD / Sharpe | 4.85 / -13.38 / 0.54 | **9.44 / -12.62 / 0.86** |

**配对统计（预注册）**：A 股挖掘期 受影响 71.8%、配对中位 **+9.98**、改善 **76.6%**、符号检验 **p=1.8e-14**、配对均值 +17.42；A 股验证期 79.6%、**+20.72**、**85.7%**、**p=9.1e-29**、+32.43。**两期中位数与均值同号。**

**决定性的对照**：候选战胜全部四个对照，A 股验证期 **+21.50 ~ +24.54**；**而同仓位随机对照本身劣于基线**（中位 -3.94/-3.22 对 -1.79）。**这是本项目首次出现"择时本身在起作用、而非多买仓位"的结果**——此前所有候选的增益都可由仓位解释。

**三条限定，必须与结论同时引用：**
1. **港股事先声明不可判定，实测确实边缘**：验证期 p=0.098 不显著，组合回撤由 -15.78 恶化到 -20.49。**这是一条 A 股规则。**
2. **对纯 dip 对照是 3/4 而非 4/4**（A 股挖掘期 **-6.53**，纯 dip 更好）。但仓位差距决定性：纯 dip 跑在 **69.44%** 仓位、组合回撤 **-23.47**，候选 **46.16%** 仓位、**-12.62**。**检测器买到的是风险效率而非原始收益**，与两个开发池结论一致，在 pool_c 上分离更清楚。
3. **验收无法覆盖崩塌股接刀的缺陷**（00909：正式策略 +152% → 叠加后 -58%）。三个池全部不含退市股，真实尾部更重。三种护栏（MA120 斜率、MA250、腿数/连亏熔断）实测全部得不偿失。

### pool_c：唯一剩余的未揭示验收池（2026-09-11 建成，当日花掉）

`stock_trading_advisor/data/pool_c/`，**379 只（280 A 股 + 99 港股）**，窗口与其余三池完全相同，34MB。选取仅用流动性与数据质量（Yahoo 筛选器快照、市值等数十分位分层、`default_rng(20260911)`），与 `codes_305`、`codes_oos_230`、pool_b 全集**各零重叠**；逐文件完整性检查 **0 违规**；`research/out` 中 pool_c 标签产物数 **0**——**从未跑过任何回测**。

注册需在 `resolve_pool()` 加 `"pool_c"` 到现有 pool_b 分支的元组里（一行）。

**花掉它之前必须知道的四条**：① 它**不是独立复制**——与 pool_b 同一张 2026-09-11 当前上市快照、同样**无退市股**（预判叠加是买跌规则，从该偏差获益最大）、同样的期末流动性筛；按本项目自测的零假设，干净池间相关 +0.90/+0.95，**pool_c 只值一次额外的独立检验**。② 港股仅 99 只（第 10 分位全空），流动性下限 2m HKD 低于 oos（10m）与 pool_b（5m），而基线在 20bp 下中位数已为负——**港股结论在 pool_c 上视为不可判定**。③ 约 12 只（3.2%）是已用池公司的 A+H 对应上市，新信息接近零。④ 密封是**程序性**的：`resolve_pool()` 末尾 `return name`，传绝对路径即可绕过。

**验收条件已预先写定（见本轮工作流最终报告第 8 节），一次性、单批、不调参；任何重跑或改参即视为花掉该池。**

### 前瞻叠加层已合入正式策略，及四路独立审计的订正（2026-09-12）

**合入内容**：`new_strategy.py` 新增 `ANTICIPATION_HOLD=20` / `ANTICIPATION_STOP=12.0`、`_anticipation_signal()`、`_apply_anticipation_overlay()`；新增 `src/launch_features.py`（176 行，仅 OHLCV）与 `src/launch_model.json`（507 行冻结系数）。叠加层在主仓位序列算完**之后**作用，只在主策略空仓时补仓，**`market in ("HK","US")` 时整体关闭**。`research/hooked.py` 已加 `ANTICIPATION_ENABLED` 开关同步（自带叠加的候选设为 False 以免叠两次）。

**Performance (ALL305)**：Return 138.76→**210.63**、Median 43.77→**64.32**、tPF 1.94→**2.03**、Win 37.67→**39.86**、MaxDD -48.89→**-49.30**；组合 CAGR 12.40→**15.49**、MDD -11.77→**-14.90**、Sharpe 1.15→**1.29**。

**必须订正的三处（此前记录有误）：**
1. **组合三元组 `18.52 / -17.93 / 1.24` 是错的**——那是港股也开启叠加的研究候选。出厂实测为 **15.49 / -14.90 / 1.29**。组合回撤确实恶化，但幅度是 3.13pp 而非 6.16pp。
2. **"2 只亏 >100pp 且都仍为正"是错的**：**300010 由 +288.17% 变为 -1.26%，已转负**。
3. **尾部在另外两个干净池上远重于 pool_c**：P(Δ<-50pp) pool_c 0.7%、**oos 4.0%、dev305 3.9%**；最差单股 Δ 分别 -289 / -280 / **-646pp**。

**验收强度必须按有效样本引用，不得只报 p 值。** 该叠加层的逐股日超额池内两两相关 pool_c 0.1255 / pool_b_dev 0.1322 / **跨池 0.1296（三者相同）**，故有效独立观测 pool_c 仅 **7.78**、两池合并 **7.67**——**pool_c 只增加 0.35 个有效观测**。四池等权日超额序列相关 0.957–0.967。修正后：符号检验 **7/8，p=0.070**；年度 t 检验 **t=2.23，p=0.067**；年度超额 95% 区间 **[-0.41, +8.91] pp/年，含 0**。**真实强度约 2σ，不是 p=9.1e-29。**

**比过拟合更关键的事实：效应集中在三个月。** pool_c A 股 6.7 年总超额 +29.74pp，其中 **2024-02 +8.96、2022-05 +6.01、2026-08 +4.52 = 65.5%**；**超额最高的 20 个交易日 ≈ 96.5%**；81 个月仅 43 个月为正，**月度中位数 0.000**；剔除 2022 与 2024 两年只剩 **+1.68 pp/年**。**三个池给出同样那三个月**（oos 70.5% / pool_b_dev 70.3%）——**不是 280×3 次验证，是同一批市场级恐慌底被复制三遍**。

**诚实的落地预期：常态年份 +1.2~1.8 pp/年；仅在再现 2022-05 / 2024-02 级别全市场恐慌底时才有 +6~9pp。** 滚动 12 个月窗口 9.9% 为负，最差 **-4.72pp**。

**加分项**：探测器系数冻结于 2023-10-02，**冻结后贡献 +19.65pp、冻结前仅 +10.10pp**。端口一致性：生产类与研究候选在 **280/280 只 A 股 0 仓位差异、max|Δ收益| = 0.0000000000**；港股 99/99 等于合并前基线。

**未来函数：无泄漏，三层独立检测。** 61 个特征逐行审查（0 负位移 / 0 居中窗口 / 0 expanding / 0 全序列分位）；自写截断重放 **363,316 个特征单元 0 差异**；生产类全链路前缀回放 **4,421 点 0 差异**；`test_lookahead_bias_smart.py` **6/6 通过**。冻结性实证：冻结阈值在 pool_c 触发率 **8.49%**，而该数据自身 90 分位会给出**恰好 10.00%**——阈值若被重算则不可能是 8.49%。

**合入后修复的两个真 bug（均不改动任何仓位 bar，指标逐位不变）：**
1. 叠加腿平仓时未写 `exit_flags/exit_reasons`——300274 的 30 次平仓有 9 次缺失，建议流会出现"买了没卖"。
2. **更严重**：叠加腿在场期间主策略写下自己的离场标记，而合并仓位仍为 1，`get_latest_signal` 的 SELL 分支优先于 `buy_signal==1`，**导致模型仍持仓却推送"SELL"**。pool_c A 股实测 **308 根、150/280 只（53.6%）**，合并前为 0。已清除矛盾标记，实测归零。
3. 顺带修正 `launch_features` 的导入顺序，避免生产吃到 `sys.path` 上 research 的同名副本。

**上线前必须做、但回测无法证明的一件事：四个池均不含退市股，下单侧必须加 ST / 退市风险警示 / 停牌硬过滤。**

**回滚触发条件（任一成立即停）：** ① 任一在持个股出现 ≥3 次连续"更低价买入"且累计腿内亏损 < -30%，或单股相对不叠加版本落后 > 50pp；② 滚动 12 个月超额 < **-4.72pp**，或连续两个自然年超额 ≤ 0 且期间叠加腿数 > 100；③ 实盘组合回撤超出不叠加版本 3pp 以上并持续 2 个月；④ 日播出现持仓中却推送 SELL。

## 必须阅读

- 处理项目结构、主调用链、缓存规则、文件位置时，读 `docs/project-architecture.md`。
- 处理任何策略新增、替换或参数优化时，必须先读 `docs/strategy-blacklist.md`。

## 优先级

- 本文件的核心纪律优先于其他仓库文档。
- 代码事实与文档冲突时，以代码为准，并同步更新相关文档。
- 文档之间冲突时，以更具体、更新、更接近当前任务的说明为准；仍冲突时先问用户。
